---
slug: agent-teams-scaffold-guidance
status: backlog
title: Agent-teams / subagent guidance in scaffolded projects
---

# Agent-teams / subagent guidance

Future scoped investigation (re-filed from "open decisions" 2026-06-09).

## Current state

Upstream moved: the harness now ships first-class `Agent`/`Workflow` orchestration, so the old watch condition ("when agent teams moves from experimental to standard") has partly triggered.

## Open question

Should `context-engineering` scaffold subagent/workflow *guidance* into a project's rules, and if so for which project types? Not folded in yet — its own investigation; promote when a scaffolded project actually needs multi-agent structure. Scope likely medium+ (new generated guidance, possibly an intake question).

## Why (pointers)

Connects to [skill-injection-by-project-type](skill-injection-by-project-type.md) (same domain-layer surface).
