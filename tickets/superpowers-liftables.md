---
slug: superpowers-liftables
status: watching
title: Superpowers liftables — fold into incidental edits
---

# Superpowers liftables

Trigger-gated candidates from [github.com/obra/superpowers](https://github.com/obra/superpowers) (reviewed 2026-06-11). Fold each into the next *incidental* edit of the relevant file — no dedicated session (per harness-proposals council guidance).

## Candidates

- **(a) Rationalization tables** — extend "every rule cites its failure mode" with the excuse the agent actually used, harvested from recurring retro failure-tags once ~10 sessions accumulate. (The scaffold-level variant — applied to the rules `context-engineering` *emits* — lives in [scaffold-superpowers-candidates](scaffold-superpowers-candidates.md).)
- **(b) "Plan failures" enumeration** for context-engineering's ROADMAP template conventions — name the placeholder sins ("TBD", "add appropriate error handling", "similar to Task N", + "display-only stub" added 2026-06-12) + bite-sized step granularity (one 2–5-min action, exact paths, expected output).
- **(c) Word-count budgets** for always-loaded content (<150 words always-loaded, <200 frequently-loaded, <500 otherwise; verify with `wc -w`). Gives [session-start-rewrite](session-start-rewrite.md) + the CLAUDE.md-slimming work a measurable bar. 2026-06-12: a reverse-engineered ~250-char cap on skill descriptions joins the budgets — **verify before relying** (reverse-engineering, not docs); note DSB's post-D-011 description at 478 chars would be silently truncated if the cap is real.
- **(d) Instruction-priority preamble** (user instructions > skills > defaults) for scaffolded session-discipline.

## Why

Adopt the method, not the persuasion-heavy tone — our retro failure-tags don't yet show the failure classes the Iron-Law armor defends against.
