<!--
Template: AGENTS.md (flat-shape companion)
Purpose: Thin pointer to CLAUDE.md for projects using the flat shape. CLAUDE.md is canonical and carries all rules inline. This file imports it and adds one Codex-specific override.
Related principles: "Thin-pointer pair" (inverse direction), "Always-on vs path-scoped split" (flat shape).
Use this template when: rule_shape == "flat". Pairs with claude-rules-flat-CLAUDE.md.template.
-->

@CLAUDE.md

All project rules are in `CLAUDE.md` (imported above).<!-- OPTIONAL: codex_override_intro — include if uses_visual_confirmation_gate == true --> One Codex-specific override applies:

<!-- OPTIONAL: codex_visual_override — include if uses_visual_confirmation_gate == true -->
**No visual confirmation.** Codex cannot run a browser. For any UI change, run `<!-- PARAMETERIZE: check_cmd -->`, describe exactly what changed and where, and flag it for <!-- PARAMETERIZE: visual_confirmer_name --> to confirm visually before the change ships. Do not claim the UI is correct.
