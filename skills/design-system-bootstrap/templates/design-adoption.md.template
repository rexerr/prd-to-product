<!-- KEEP AS-IS: this rule is the adopt-mode invariant. Its three bullets and their failure modes are the whole point of D-008's composition-fidelity fix (presence + import-don't-rebuild). Source of record: docs/design-handoff-adoption.md, the "# Design adoption — import, don't rebuild" fence. Do not soften "never rebuild a covered surface" — that is the ~100%-drift failure it prevents. -->
<!-- PARAMETERIZE: reference_dir — where adopt copied the rendered design (default design/reference/). The default below matches the source-of-record fence; change only if the project copied the design elsewhere. -->
---
description: Adopt the Claude Design bundle's components; never rebuild a covered surface
paths: ["app/**", "components/**", "src/**"]
---
# Design adoption — import, don't rebuild

The rendered design of record lives in `design/reference/` (the bundle's pages + component source, re-snapshotted on each Claude Design export).

- **Import the bundle's components; never rebuild a surface they already cover.** Rebuilding inline requires a cited reason in the commit.
  **Failure it prevents:** components got rebuilt inline instead of imported → every page diverged in composition (~100% drift, the-council 2026-06).
- **Diff every new surface against its `design/reference/` page before commit.** No reference for a surface → add one before building it.
  **Failure it prevents:** design fidelity enforced by nothing drifts every time; token compliance alone does not hold composition.
- **A surface diffed against a stale reference is flagged, not trusted.** Re-snapshot `design/reference/` whenever the Claude Design export changes.
  **Failure it prevents:** enforcing fidelity to a design that no longer exists.

<!-- OPTIONAL: hook_deferred — emit while the mechanical intra-app-consistency hook is unbuilt (adopt v1). The first bullet's consistency check (does a new surface re-implement an imported component?) is enforced by reading this rule, not yet by a hook. Drop this block when the v2 hook ships. -->
> Mechanical enforcement of the first bullet (a hook that greps for inline re-implementations of imported components) is not yet built — until it ships, intra-app consistency is gatekept by following this rule, not automatically. Staleness (third bullet) is likewise prose, not a scaffolded check.
