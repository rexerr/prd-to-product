# Furnace-plan learning ledger

A **durable, cross-project record** of what Cowork's `/plan-review` (and, in future, other distinct reviewers) catches in `/furnace-plan`-authored plans — each row one finding, classified into a bucket — kept versioned so the furnace+reviewer loop accumulates learning across *every* project rather than scattering it across per-session retros. It serves two purposes: (1) **ongoing improvement** of the furnace's verification preflight, by surfacing which bucket-1 / bucket-2 catches recur; and (2) one specific **open sub-question** — whether the preflight has earned promotion from an invoke-skill to an `ExitPlanMode` hook, graded on the *distribution* of buckets over time, not a raw catch count. The bucket legend and promote/kill/tighten triggers below answer (2); the growing row history serves (1).

**Writer:** Cowork's `/plan-review` skill appends rows here as it reviews — one or more per round, regardless of which project the plan was for. It records its own findings (`Reviewer = cowork`) and, when a plan carries a `## Subagent review log`, transcribes the in-session blind reviewer's findings too (`Reviewer = cc-subagent`). Claude Code (the furnace/planner) never writes here. The header below is static; only the table grows.

**Subject vs. writer:** this file lives under the `furnace-plan` skill because the furnace is what it measures, even though `/plan-review` is what writes it.

---

## Buckets (the classification)

- **Bucket 1 — no-read-behind-it.** A count or "X is missing / X is named" asserted from memory or a filename that the furnace's preflight Check 1 ("every codebase claim traces to a read *this session*") *should* have forced a read to catch. → **Forcing function underperforming. Tighten the preflight; do not kill.**
- **Bucket 2 — read-but-wrong / unfalsifiable inference.** A claim the furnace believed verified — it read the source and still misread it, or made an inference no codebase read could establish. Self-review structurally cannot reach this. → **The distinct-verifier (Cowork) is load-bearing; not a kill.**
- **Bucket 3 — genuine judgment / scope / design call.** Neither the preflight nor self-review's job — correctly Cowork + Rex's. **Note: bucket 3 spans severity** — it can be a low-stakes refinement *or* a must-fix design flaw (see 06-15 R1, the over-broad trigger). That is why severity is its own column.

## How to read it for the promotion / kill decision

**Read the rubric per reviewer — load-bearing since Plan 2 added a second reviewer.** The promote/kill decision is about the *furnace preflight*, so it can only be read from the reviewer that sees **raw furnace output**: the **cc-subagent** (it reviews the plan before any fix), or **Cowork on a calibration pass** (cc-subagent skipped, raw plan sent straight to Cowork). On a normal twice-baked plan, **Cowork reviews the already-fixed plan, so its catches measure the cc-subagent's *residual* (miss-rate), not the furnace preflight** — never read Cowork's post-fix distribution as the furnace's report card. Doing so misreads pre-filtering as preflight improvement: a cc-subagent that strips bucket-1/2 leaves Cowork mostly bucket-3, which would spuriously trip "promote."

Apply the four reads below to the **raw-output reviewer's** distribution:

- **Promote (earn the hook):** catches collapse to **mostly bucket-3 *refinements*** — few/no must-fixes of any bucket. A hook that automates the preflight only helps bucket 1, so the furnace earns it once bucket 1 is gone and what's left is the judgment tier the hook was never going to touch.
- **Tighten the preflight:** **bucket 1 recurs.** The forcing function isn't being executed; a hook would just automate a half-working layer.
- **Distinct verifier stays load-bearing:** **bucket 2 persists.** The blind spot is real and a planning prompt can't reach it — the fix is a fresh-context verifier fed the ledger, not a kill, and not a hook.
- **Watch severity, not just bucket.** Mostly bucket-3 *must-fixes* means the furnace is still shipping flawed plans of a kind no mechanical layer could catch — that argues *against* the hook, not for it.

**Identifying a calibration row.** A calibration pass is just a Cowork review of raw output, so it looks like an unpaired `cowork` row (no `cc-subagent` row for that Plan label). Read a **post-Plan-2** unpaired `cowork` row as a raw/calibration furnace reading. The pre-Plan-2 rows are also raw, but graded against older preflight versions — the dated dividers separate instrument generations, so never pool pre- and post-divider raw rows into one furnace reading.

**Pair-by-plan tally (cc-subagent vs Cowork).** Compare the two reviewers only on plans **both** reviewed — the same Plan label carrying both a `cc-subagent` row and a `cowork` row. There, the `cc-subagent` rows are what the pre-filter caught early and the `cowork` rows are what leaked past it to the oracle; a small leak means the pre-filter earns its keep, a large one means it underperforms. cc-subagent is a same-model reviewer, so treat its distribution as *directional* and lean on calibration passes for a trusted furnace reading.

When the furnace preflight is ever changed (e.g. read-enforcement tightened), add a dated `### --- preflight tightened YYYY-MM-DD ---` divider row so before/after trend is visible without a version column.

Full rationale and triggers: `~/Sites/prd-to-product/BACKLOG.md` (furnace-plan trial, In progress) and `docs/DECISIONS.md`.

---

## Ledger

| Date | Project | Plan | Round | Bucket | Severity | What the reviewer caught | Reviewer |
|---|---|---|---|---|---|---|---|
| 2026-06-14 ~10:45 CDT | prd-to-product | chain-handoffs D-014/D-015 | 1 | 1 | must-fix | claimed new decision = D-013; already taken (skipped the read-the-log step) |
| 2026-06-14 ~10:45 CDT | prd-to-product | chain-handoffs D-014/D-015 | 1 | 2 | must-fix | "DSB never named in either file" — false; context-engineering/SKILL.md:59 names it (read but misread) |
| 2026-06-14 ~10:45 CDT | prd-to-product | chain-handoffs D-014/D-015 | 2 | 3 | refinement | `notes.md` — invented filename BACKLOG never specified |
| 2026-06-14 ~10:45 CDT | prd-to-product | chain-handoffs D-014/D-015 | 2 | 3 | refinement | chain-composition path-match should be named explicitly in D-015 |
| 2026-06-14 ~10:45 CDT | prd-to-product | chain-handoffs D-014/D-015 | 2 | 3 | refinement | temporal-claim check missing from verification |
| 2026-06-15 ~11:30 CDT | prd-to-product | red-team Rung 1 handoff | 1 | 2 | must-fix | conditional critique copy in always-printed Format block while claiming examples stay valid *because* it's conditional — internal contradiction |
| 2026-06-15 ~11:30 CDT | prd-to-product | red-team Rung 1 handoff | 1 | 3 | must-fix | over-broad trigger ("any V1/V2 cut") fired on every PRD — collapsed conditional into mandatory |
| 2026-06-15 ~11:30 CDT | prd-to-product | red-team Rung 1 handoff | 2 | 3 | refinement | fill-slot instantiation / sharper identity-defining cue / verification-scope clarification |
| 2026-06-17 (1st session) | prd-to-product | skill-frontmatter validation hook (C-27/D-017) | 1 | 2 | must-fix | [backfilled] fail-open lockout: a missing/mis-pathed `skills/` SKILL.md would block every commit — hook must fail open |
| 2026-06-17 (1st session) | prd-to-product | skill-frontmatter validation hook (C-27/D-017) | 1 | 2 | must-fix | [backfilled] forgeable whole-file field grep: `name`/`description` matched anywhere in the file, not only inside the frontmatter block |
| 2026-06-17 (1st session) | prd-to-product | skill-frontmatter validation hook (C-27/D-017) | 1 | 2 | must-fix | [backfilled] unterminated-frontmatter pass-through: a block missing its closing `---` fence passed validation |
| 2026-06-17 (1st session) | prd-to-product | skill-frontmatter validation hook (C-27/D-017) | 2 | 3 | refinement | [backfilled] missing-closing-fence case — judgment-tier improvement (the converge-toward-bucket-3 pattern held) |
| 2026-06-17 (2nd session, ~16:39 CDT) | prd-to-product | furnace-plan migration | 1 | 3 | must-fix | [backfilled] `mv` on the only unbacked copy is irreversible if `~/.claude` and `~/Sites` are cross-volume (mv→copy+unlink); use cp → verify (`diff -r`) → commit → rm → symlink instead |
| 2026-06-17 (2nd session, ~16:39 CDT) | prd-to-product | furnace-plan migration | 1 | 3 | refinement | [backfilled] README note that the install loop "covers" `context-engineering-audit` contradicts same-day D-019; mention only furnace-plan, file the loop-vs-D-019 tension separately |
| 2026-06-17 (2nd session, ~16:39 CDT) | prd-to-product | furnace-plan migration | 1 | 3 | refinement | [backfilled] 2nd "lives outside this repo" in BACKLOG sits in a "recorded here per where-facts-live" parenthetical that goes obsolete post-move; sentence-level rewrite, not a blanket string swap |
| 2026-06-17 (2nd session, ~16:39 CDT) | prd-to-product | furnace-plan migration | 1 | 3 | refinement | [backfilled] Cowork appends rows but can't commit; without a sweep-into-next-commit convention the "durable/versioned" goal is only met at commit time |
| 2026-06-17 (2nd session, ~16:39 CDT) | prd-to-product | furnace-plan migration | 2 | 3 | refinement | [backfilled] keep the commit-sweep convention worded narrowly to `trial-ledger.md`, not a general "commit changes you didn't make" rule (footgun) |
| 2026-06-17 (2nd session, ~16:39 CDT) | prd-to-product | furnace-plan migration | 2 | 3 | refinement | [backfilled] at D-020 write time, confirm the DECISIONS_ACTIVE mirror doesn't duplicate D-018's existing ACTIVE entry on the Cowork-write rule |
| 2026-06-17 ~20:40 CDT | prd-to-product | CF-03 red-capable-repro gate | 1 | 2 | refinement | verify-step-3 ledger claim that new path token "matches line 72" is imprecise — line 72 uses qualified `skills/context-engineering/examples/output-small/`, new wording uses bare `examples/output-small/`; only a substring match, not the verbatim match the step asserts |
| 2026-06-17 ~20:40 CDT | prd-to-product | CF-03 red-capable-repro gate | 1 | 3 | refinement | rule grows 1 line → ~6 sentences in always-loaded CLAUDE.md (the file the repo's own CF-01 note diagnoses as sprawl-prone); tighten wording, esp. the thin half-(b) debug-log clause |
| 2026-06-18 ~12:00 UTC | prd-to-product | C-09+CF-06 retro cluster (D-024) | 1 | 3 | refinement | sync-marker edit under-specified: plan says "refresh date" but CF-03 precedent records the adoption event in the line-1 marker — append "C-09 + CF-06 adopted (→D-024)" and use 2026-06-18, not a bare date bump |
| 2026-06-18 ~12:00 UTC | prd-to-product | C-09+CF-06 retro cluster (D-024) | 1 | 3 | refinement | tracker-cell flips quoted only the "N adopted" fragment; instruct edit-in-place of that fragment, preserving "Mining complete; …rest standing" (row 15) / "Mined + 6-lens re-mined…rest Proposed" (row 16) instead of overwriting the whole cell |
| 2026-06-18 ~15:40 UTC | prd-to-product | Wave-2 Sprint 1 furnace ledger trio | 1 | 2 | must-fix | ledger claim "G-16/C-16/CF-15 all land on the verification-ledger region" overstates: re-read of SKILL.md shows only G-16 lands on the ledger (lines 49-57); C-16 targets the sign-off prose at "## What this is not" (line 65), CF-15 targets "## The preflight" (lines 25-43) — three distinct edit surfaces, not one |
| 2026-06-18 ~15:40 UTC | prd-to-product | Wave-2 Sprint 1 furnace ledger trio | 1 | 3 | must-fix | CF-15 risks a no-op here: its failure mode (blended PASS hiding a one-axis defect) has no referent — furnace emits no verdict today (preflight is a pass/fix gate, ledger is a flat claim list); same collapse-into-existing-checks pattern that REFUTED G-02. Confirm new structure/defect before adopting |
| 2026-06-18 ~15:40 UTC | prd-to-product | Wave-2 Sprint 1 furnace ledger trio | 1 | 3 | refinement | dogfood G-16 on this plan's own ledger: 5 open sign-off items but the ledger doesn't terminate in the mandatory sentinel — applying the rule to the plan tests its shape and answers sign-off item 1 (terminal block vs separate section) |
| 2026-06-18 ~15:40 UTC | prd-to-product | Wave-2 Sprint 1 furnace ledger trio | 1 | 3 | refinement | D-NNN granularity: adopting 3 cribs (D-031/032/033) in one pass is the most-at-once vs the D-022 "don't batch-adopt" lifecycle; weigh against the D-024 one-shared-D precedent |
| 2026-06-18 ~15:40 UTC | prd-to-product | Wave-2 Sprint 1 furnace ledger trio | 1 | 3 | refinement | CF-23 home: skill-md-template.md self-describes as "not a binding spec"; if the two-load rule should bind invocation choices it belongs in DECISIONS, template carrying only the explanation |
| 2026-06-18 ~15:47 UTC | prd-to-product | Wave-2 Sprint 1 furnace ledger trio | 2 | 3 | refinement | CF-23 is the sprint's likeliest DECISIONS_ACTIVE mirror candidate (forward-looking authoring rule, not code-visible, live /repo-miner consumer) — evaluate specifically, lean mirror-yes; others stay DECISIONS-only |
| 2026-06-18 ~15:47 UTC | prd-to-product | Wave-2 Sprint 1 furnace ledger trio | 2 | 3 | refinement | Pass C: the D-034 record IS the CF-23 binding rule — write one DECISIONS.md entry, not a separate "adoption" + "rule" pair; template carries only the pointer |
| 2026-06-18 ~15:47 UTC | prd-to-product | Wave-2 Sprint 1 furnace ledger trio | 2 | 3 | refinement | if G-01 folds into Pass A, mark it Adopted-as-REVISED in the gstack tracker (verbatim-quote clause only; confidence-cap machinery + framework-meta nudge stay dropped) — it's currently REVISE, not Proposed |
| 2026-06-18 ~16:20 UTC | prd-to-product | Wave-2 Sprint 2 prd-creator cribs (CF-18/G-06/G-08) | 1 | 3 | must-fix | G-06 placement won't fire at runtime: plan puts the banned-hedge list + posture body in principles.md, but SKILL.md line 50 + principles.md line 3 both say principles.md is NOT loaded per-invocation. G-06 is a per-turn interview rule; its actionable content must live in intake.md (the file loaded at trigger), not behind a one-line cold pointer. Asymmetry the plan misses — it correctly puts CF-18 rule 2 in intake.md but G-06's list in the non-loaded file |
| 2026-06-18 ~16:20 UTC | prd-to-product | Wave-2 Sprint 2 prd-creator cribs (CF-18/G-06/G-08) | 1 | 3 | must-fix | bounded CF-18 rule 1 collides with the flagship small transcript + regression test #11, not as an edge case: read examples/small/transcript.md — first 3 questions are bare cold free-text. "Offer a candidate even when cold" applied to every question puts the primary cold-path fixture in violation from Q1, failing verification step 4 pervasively. Rule must be scoped (material-covered + branching/decision questions only; open free-text stays bare) or fall back to sign-off option (c). Plan/sign-off understate this as "log a follow-up" |
| 2026-06-18 ~16:20 UTC | prd-to-product | Wave-2 Sprint 2 prd-creator cribs (CF-18/G-06/G-08) | 3 | 3 | refinement | Pass A rule 3 (user-claim-vs-source-material contradiction → forced choice) scoped only to cluster-0 draft-and-present, but contradictions between a later user answer and the brief surface across all clusters; belongs in general "How to run a cluster" shape, not cluster 0 alone |
| 2026-06-18 ~16:20 UTC | prd-to-product | Wave-2 Sprint 2 prd-creator cribs (CF-18/G-06/G-08) | 3 | 3 | refinement | G-08 output-summary.md line should be framed as resolution-provenance ("flagged X; you kept/removed it") like existing longer-summary cases 2/3, not a live unresolved-leak warning — summary prints post-write, after the pre-write scan is already resolved |
| 2026-06-18 ~16:45 UTC | prd-to-product | Wave-2 Sprint 2 prd-creator cribs (CF-18/G-06/G-08) | 2 | 2 | refinement | latent: rule 1(b)'s "recommended option" on a genuine undecided *product* branching decision edges toward the rejected literal form; bind it by the same anti-invention guard as 1(a) (recommend among presented options, marked default-to-edit, never originate a direction). Low severity — grep of all 3 transcripts shows scripted clusters pose ~no branching product questions, so 1(b) rarely bites today; guard the wording before a future cluster adds one |
| 2026-06-18 ~17:15 UTC | prd-to-product | Wave-2 Sprint 3 retro-template backport (C-09/CF-06/C-14) | 1 | 2 | must-fix | Ledger claimed verification = "literal diff of `tail -n +7 template` vs fixture; no substitution logic" — re-ran it: FALSE. Template has a blank line 7 between the `-->` (line 6) and `# Session retros` (line 8); fixture has no leading blank. `diff <(tail -n +7 …) fixture` prints `1d0 <` (blank) in the CURRENT clean state, so the plan's "GREEN = empty" can never be reached even when edits are correct. Correct command is `tail -n +8`. Risk: implementer "fixes" the persistent diff by mangling the blank line in template or fixture |
| 2026-06-18 ~17:15 UTC | prd-to-product | Wave-2 Sprint 3 retro-template backport (C-09/CF-06/C-14) | 1 | 3 | refinement | Repo reference retro README (the mirror source) diverges from template/fixture on the Open-items line (`PARKING_LOT.md or ROADMAP.md` vs `BACKLOG.md (In progress / Backlog)`) and keeps `bad substitution` in its Tag set; plan correctly changes only the 3 named blocks but should state explicitly: apply ONLY the 3 edits, do not reconcile the Open-items line or copy the repo's tag set, or the template↔fixture diff breaks |
| 2026-06-18 ~17:19 UTC | prd-to-product | Wave-2 Sprint 3 retro-template backport (C-09/CF-06/C-14) | 2 | — | — | clean pass, no findings — both round-1 items resolved: verification corrected to `tail -n +8` (re-ran: empty/exit 0), and the explicit "apply only the 3 edits / leave the PARKING_LOT+bad-substitution divergences" guard added |
| 2026-06-18 ~18:40 UTC | prd-to-product | Wave-2 Sprint 3 DG-01 synthesis even-coverage rule | 3 | 1 | must-fix | Under-enumerated suppression surfaces: plan claims medium suppression is "a prose sentence, line 38" and large is one "Not emitted" bullet, but `grep -n product-rules` shows the parallel conditional appears TWICE in each abbreviated file — medium at line 37 (prose) AND line 112 (`## What's not present` bullet); large at line 43 (`Not emitted`) AND line 139 (`Always-on rules … (No product-rules.md.)`). Plan's edit instructions hit one per file, leaving the new rule's fixture parity incomplete at line 112 / 139. Same under-enumeration class as the round-1 catch, smaller scale |
| 2026-06-18 ~18:40 UTC | prd-to-product | Wave-2 Sprint 3 DG-01 synthesis even-coverage rule | 3 | 1 | must-fix | State-map registry gap: plan edits only the Per-template inclusion table + a gating subsection in decisions.md, but the State map table at decisions.md:9–45 registers the parallel flag `include_product_rules` (line 42) and the new `include_synthesis_rule` is not added there — registry left incomplete |
| 2026-06-18 ~18:40 UTC | prd-to-product | Wave-2 Sprint 3 DG-01 synthesis even-coverage rule | 3 | 3 | refinement | Marker-map registration unscoped: "Register the state key in Notes/marker-map" must land in intake.md's `### Cluster 6` subsection (line 361+), not the Cluster 5 block (357) where the parallel `include_product_rules` sits — Q35a is a Cluster-6 question post-rev-4 |
| 2026-06-18 ~18:40 UTC | prd-to-product | Wave-2 Sprint 3 DG-01 synthesis even-coverage rule | 3 | 3 | refinement | Classification unstated: written template has no `paths:` frontmatter → always-on-when-emitted (like product-rules), so the correct large-output sibling is the always-on absence note (line 139), not the path-scoped list (137); confirm `path_scoped_rule_list` (decisions.md:360–368) excludes it |
| 2026-06-18 ~18:40 UTC | prd-to-product | Wave-2 Sprint 3 DG-01 synthesis even-coverage rule | 3 | 3 | refinement | Stale label: Sign-off 2 recommendation still reads "Q27c gated to rule_shape" while rev-4 renamed the question Q35a everywhere else — cosmetic (Sign-off 2 RESOLVED) but two names for one question |
| 2026-06-18 ~19:05 UTC | prd-to-product | Wave-2 Sprint 3 DG-01 synthesis even-coverage rule | 4 | 3 | must-fix | rev-5 added a correcting banner (MF1 lines 37+112 / 43+139, MF2 State-map row, SC marker-map/Q35a) — all re-verified correct against files — but did NOT update the body: §B.7 still says "line 38 prose, add a matching line" (one spot, and 38 is wrong → 37); §A.2 omits the State-map row; Sign-off 2 still "Q27c". Banner-over-stale-body conflict risks the implementer following §B.7's single-location version and reintroducing the MF1 inconsistency. Reconcile body to banner |
| 2026-06-18 ~19:05 UTC | prd-to-product | Wave-2 Sprint 3 DG-01 synthesis even-coverage rule | 4 | 1 | refinement | rev-5 banner cites `path_scoped_rule_list` at decisions.md:386, but that section is decisions.md:360–368; line 386 is the PRD-driven area (the :386 is intake.md's marker registration). Line-number citation not traced to the actual section |
| 2026-06-18 ~19:40 UTC | prd-to-product | Wave-2 Sprint 3 DG-01 synthesis even-coverage rule | 5 | — | — | clean pass, no findings — POST-IMPLEMENTATION review: plan already built & committed at c1a1731. Re-verified the committed files against the rev-5 banner + every ledger claim: Q35a lands in Cluster 6 (intake.md:238) after rule-shape determination, State-map row (decisions.md:43), inclusion row (229), gating subsection (165-169), template has no PARAMETERIZE/paths markers + conditional header + cites failure mode, all four modular fixtures suppress at both product-rules sites (medium 37/113, large 44/140), transcript-small byte-untouched, D-040 present & not mirrored to DECISIONS_ACTIVE, path_scoped_rule_list correctly excludes the always-on rule. All correct. Not a real pre-build gate |
| 2026-06-18 ~19:55 UTC | prd-to-product | Wave-2 Sprint 3 CF-13 dynamic final-step self-exercise | 1 | 2 | must-fix | Verification rests on a false premise: plan's RED/GREEN both assert a dry-run substitution of the flat template diffs "clean (no new bullet)" against output-small/CLAUDE.md. But CLAUDE.md:23 says examples are hand-written, and structurally the flat template's Session discipline has 11 ### subsections (lines 83-147) while output-small collapses them to a 14-bullet list (42-57), Verification compressed to the single line-50 bullet. A real substitution differs across the whole section, never "only by the new bullet" — so the stated pass condition is unreachable, and a literal attempt risks the implementer expanding output-small to match the template verbatim (whole-section rewrite, breaks the compressed-example convention) |
| 2026-06-18 ~19:55 UTC | prd-to-product | Wave-2 Sprint 3 CF-13 dynamic final-step self-exercise | 1 | 3 | must-fix | Modular template placement: new bullet must be unconditional, but section ends with `<!-- OPTIONAL: tooling_check — include if has_check_script == true -->` (line 90) + tooling bullet (91), no closing marker / no blank line. Per decisions.md:271,276 OPTIONAL gates "next line or next contiguous block ending at a blank line" — "append" after 91 risks the new bullet being dropped when has_check_script==false. Must place outside any OPTIONAL block and prove with a gates-off (visual=false AND check=false) substitution |
| 2026-06-18 ~19:55 UTC | prd-to-product | Wave-2 Sprint 3 CF-13 dynamic final-step self-exercise | 1 | 3 | must-fix | Flat template same OPTIONAL-placement risk: verification_ui_bullet opens at line 123 (no closer), bullets 124-125 contiguous, blank at 126 — appending after 125 risks the new bullet being gated by uses_visual_confirmation_gate. Place before the marker (after the "Never claim success" sentence, line 121) and verify with the visual gate off |
| 2026-06-18 ~19:55 UTC | prd-to-product | Wave-2 Sprint 3 CF-13 dynamic final-step self-exercise | 1 | 3 | refinement | decisions.md:269-278 doesn't cover nested/unclosed OPTIONAL markers, yet both templates already place the unconditional "Never claim success" content in a contiguous block after an OPTIONAL marker — so the real parse must be finer than "block-to-blank-line." Reviewer can't settle this by reading; flagged that the gates-off substitution is the only authority, don't reason it through |
| 2026-06-18 ~20:25 UTC | prd-to-product | Wave-2 Sprint 3 CF-13 dynamic final-step self-exercise | 2 | 2 | refinement | rev-2 verification §1 claims the gates-off render "empirically settles the nested-OPTIONAL parse question rather than reasoning from the spec" — overstated. CLAUDE.md:23 ("no package manifest, server, linter, or test runner") + :77 ("the dry-run substitution IS the verification") show there is no executable generator: the "render" is an LLM re-applying the same ambiguous decisions.md prose, so it's a self-consistency check, not independent settlement. Mitigated (not must-fix) because the before-the-marker placement is ungated under BOTH readings of decisions.md:276; advised reframing the load-bearing pass condition to just "new bullet survives" and treating the secondary drop-observations as parse-dependent, not hard fail criteria |
| 2026-06-18 ~20:25 UTC | prd-to-product | Wave-2 Sprint 3 CF-13 dynamic final-step self-exercise | 2 | 3 | refinement | rev-2 out-of-scope flag (modular line 89 "Never claim success" gated by verification_ui_bullet, "likely drops" for no-UI) is parse-conditional: real only under contiguous-block gating; the marker name verification_ui_bullet (singular) hints single-line gating, in which case 89 survives and there's no bug. Plan hedges "likely" appropriately but should record D-041/BACKLOG note as a parse-conditional candidate, not confirmed. Also missed parallel exposure: flat template logic bullet (line 125) shares the same verification_ui_bullet gate (marker 123, contiguous to blank 126), so a no-visual flat project may lose "Logic: run check, report results" too — future fix should cover both surfaces |
| 2026-06-19 ~04:08 UTC | prd-to-product | plan-review rehost + decision-section rework | 1 | 2 | must-fix | A2 + the verification ledger both assign the new hosting decision **D-040**, claiming "highest existing = D-039 (grep -oE D-0[0-9]{2} docs/DECISIONS.md)." Re-ran that grep on the synced repo: D-040 already exists at DECISIONS.md:365 ("DG-01 adopted: even-coverage synthesis rule", 2026-06-18), and DECISIONS_ACTIVE.md:1 reconciliation marker reads "through D-040." The ledger's read was stale/wrong. Next free number is D-041 (verified absent), though CF-13 furnace work has informally earmarked D-041 in this same ledger — re-check at write time. Same decision-number-collision class D-020's retro caught as "stale D-018→D-020 id" |
| 2026-06-19 ~04:08 UTC | prd-to-product | plan-review rehost + decision-section rework | 1 | 3 | refinement | A2 mirrors the new decision into DECISIONS_ACTIVE.md but omits bumping that file's line-1 reconciliation marker ("Reconciled against DECISIONS.md through D-040 · 2026-06-18"), whose own instruction is "Bump this line whenever a new decision is evaluated for mirroring, mirrored or not." Add the marker bump as an explicit step. Also: D-040 itself was deliberately NOT mirrored (template/generator content) — plan should state it mirrors because this is a binding cross-tool hosting constraint matching D-020's precedent, not the D-040 skip precedent |
| 2026-06-19 ~04:14 UTC | prd-to-product | plan-review rehost + decision-section rework | 2 | 2 | must-fix | UNCHANGED from round 1 and now worse: plan still assigns D-040 throughout (A2, ledger, Sign-off, BACKLOG). D-040 was already taken (DECISIONS.md:365); since round 1, CF-13 also landed **D-041** (DECISIONS.md:373). Next free number is now **D-042** (verified absent). The revised plan made no edit to the number — the round-1 must-fix was not applied. Re-check at write time |
| 2026-06-19 ~04:14 UTC | prd-to-product | plan-review rehost + decision-section rework | 2 | 3 | refinement | UNCHANGED from round 1: A2 still omits bumping DECISIONS_ACTIVE.md line-1 marker. Now compounded — the marker already reads "through D-040" while D-041 exists (D-041 didn't bump it either), so it's pre-existing-stale; this plan should bump it to D-042 and is the chance to correct the drift |
| 2026-06-19 ~04:18 UTC | prd-to-product | plan-review rehost + decision-section rework | 3 | 3 | must-fix | Phase 2a's reword list (Context, Hard rule, "synced", "revise field/box" at L11/14/33) OMITS the frontmatter `description:` at L3, which contains "Produces paste-ready feedback for Claude Code's revise box" — a Cowork/paste-bound phrase. But Phase 2a's own verification grep ("revise box" literal → RED) WILL match L3, so the implementer is caught between an edit list that ignores L3 and a RED check that fails on it. Compounded: L3 is the skill's trigger `description`, and CLAUDE.md says "Do not silently change SKILL.md trigger phrases." Plan must explicitly decide L3's fate and, if reworded, flag it as a sanctioned trigger-phrase edit (keywords unchanged, only the host tail) |
| 2026-06-19 ~04:18 UTC | prd-to-product | plan-review rehost + decision-section rework | 3 | 2 | refinement | Phase 2b cites C-16 as "(adopted, D-016-era)". C-16 is genuinely adopted and its text matches the plan, but via **D-031** (steinberger tracker L117: "Adopted (→ D-031)"; roadmap Sprint-1), NOT D-016 (which is the unrelated red-team rung-1 decision). The new D-042 entry / 2b reference should cite D-031 as C-16's adoption precedent, not D-016 |

### --- preflight tightened 2026-06-18 --- Check 1 gained sub-case 1a (string-equality & decision-number claims: quote both sides, re-read at write time), triggered by the D-040→D-042 decision-number collision above (rows 06-19 ~04:08/04:14, logged bucket-2 / read-but-stale). Rows below are post-tightening.

| 2026-06-19 ~12:22 UTC | prd-to-product | Plan 2 blind in-session reviewer loop | 1 | 3 | must-fix | Read-from-disk mechanic has no plan file to read: furnace SKILL.md (grep: no write/disk/path mention) presents the plan via ExitPlanMode held in conversation, never writes it to disk. Plan 2 says subagent reads "the plan file just written" but never adds the write step. Worse, writing that file is a MAIN-agent write during plan mode — the exact write-in-plan-mode capability the plan's own ledger marks UNVERIFIED (it resolved that only for the subagent by keeping it read-only, and missed that the main agent must write the plan file for the read-only subagent to read it). Probe as step 0; if main-agent can't write in plan mode the read-from-disk handoff collapses |
| 2026-06-19 ~12:22 UTC | prd-to-product | Plan 2 blind in-session reviewer loop | 1 | 3 | must-fix | Second reviewer confounds the promotion/kill rubric, which the plan doesn't update. trial-ledger.md "How to read it for the promotion / kill decision" (lines 17-22) reads Cowork's bucket distribution to decide promote/tighten/kill. A cc-subagent that pre-filters bucket-1/2 catches shifts Cowork's residual rows toward bucket-3 artificially → spuriously trips "mostly bucket-3 → promote the hook" when the furnace preflight never improved. Plan adds a tally rule but leaves the promotion-reading section unchanged. Update that section to read the rubric per-reviewer, or the experiment misreads its own promote/kill call |
| 2026-06-19 ~12:22 UTC | prd-to-product | Plan 2 blind in-session reviewer loop | 1 | 3 | refinement | DECISIONS_ACTIVE.md line-1 reconciliation marker bump omitted — same miss caught twice already (rows 06-19 ~04:08 / ~04:14). Marker currently reads "through D-042 · 2026-06-18"; mirroring D-043 must bump it to D-043. Plan's file table lists the D-043 mirror but not the marker bump |
| 2026-06-19 ~12:22 UTC | prd-to-product | Plan 2 blind in-session reviewer loop | 1 | 3 | refinement | Reviewer-rename leaves stale "Cowork" naming: renaming the column to "What the reviewer caught" but leaving the trial-ledger legend ("Writer: Cowork", "What Cowork caught" in How-to-read) and plan-review SKILL.md's column-description bullet (line ~105) saying "Cowork" creates header-vs-prose inconsistency. Sweep the naming, not just the header cell |
| 2026-06-19 ~12:39 UTC | prd-to-product | Plan 2 blind in-session reviewer loop | 2 | 3 | refinement | Rubric repair (Must-fix 2, rev 2) is sound but under-specifies how a future reader IDENTIFIES a cc-subagent-off "calibration" Cowork row: the per-reviewer rubric says read the furnace signal from "Cowork on cc-subagent-off passes," but there's no column marking such passes — they're just unpaired cowork rows, which also describes all 58 pre-Plan-2 historical rows (raw, but from older preflight versions). Rubric should state the identification rule explicitly (post-Plan-2 unpaired cowork row = calibration; use the dated dividers to separate instrument versions) or the raw-furnace reading silently pools two preflight generations |

### --- Reviewer column + blind cc-subagent reviewer added 2026-06-19 (Plan 2, D-043) --- The ledger gained a `Reviewer` column (`cowork` | `cc-subagent`), placed **last** so existing rows need no backfill: every row above this divider predates the column and is **`cowork`** (an empty Reviewer cell = `cowork`). The furnace also gained an in-session blind cc-subagent reviewer (`furnace-plan/SKILL.md`, "The blind review"), so from here Cowork no longer sees raw furnace output on twice-baked plans — read the promotion rubric **per reviewer** (above). New rows fill all 8 columns.
| 2026-06-19 ~12:39 UTC | prd-to-product | Plan 2 blind in-session reviewer loop | 2 | 3 | refinement | Sign-off 1 option (b) is described as "the cc-subagent writes its own row," but the cc-subagent is a read-only Explore agent that has already returned during plan mode — it structurally cannot write, and is gone by ExitPlanMode. (b) would actually require a separate write-capable agent (or the main agent) writing post-ExitPlanMode. Not blocking since (a) is recommended, but clarify (b)'s real mechanism so it isn't mis-implemented if Rex picks it |
| 2026-06-19 ~13:24 UTC | prd-to-product | DSB adopt mode v1 | 1 | 3 | must-fix | decisions.md "What the generator never writes" (197-208) is a CLOSED allowlist — "This skill writes only files at these paths: […]" + "Never write files anywhere else." Adopt writes two paths absent from it: design/reference/** and .claude/rules/design-adoption.md. Plan file-3 only says "add an entry clarifying adopt copies but never authors" — but a cp is still a write-by-path, so unless the positive list (199-206) AND the "Never write files anywhere else" line (208) are both amended to admit the adopt paths, the governing generator doc self-contradicts after the change | cowork |
| 2026-06-19 ~13:24 UTC | prd-to-product | DSB adopt mode v1 | 1 | 3 | refinement | docs/design-handoff-adoption.md is not in the plan's edit set, but its lines 1/49/58 assert "Per D-008 the kit builds no design-system-bootstrap adopt-mode" / "no prd-to-product skill is built for this" / "Nothing here modifies prd-to-product" — all FALSE once adopt ships. Plan keeps this doc verbatim as the manual fallback (Q2), so its "no adopt-mode" claims contradict reality; add a superseding note at top pointing to adopt mode / D-044. Verification check 5 won't catch it (doc isn't "edited") | cowork |
| 2026-06-19 ~13:24 UTC | prd-to-product | DSB adopt mode v1 | 1 | 3 | refinement | Verification check 4 ("emitted rule matches design-handoff-adoption.md:64-79, RED on any drift") is in tension with file-5's planned PARAMETERIZE of the component import dir — a parameterized template can't byte-match the static 16-line draft for a non-default project. Specify check 4 diffs the DEFAULT/example emission (paths app/components/src as drafted), not byte-identity across all param values, or implementer drops parameterization to pass the check | cowork |
| 2026-06-19 ~13:24 UTC | prd-to-product | DSB adopt mode v1 | 1 | 3 | refinement | Plan file-1 "add adopt trigger phrasings" doesn't say whether SKILL.md frontmatter description: (line 3) gains adopt keywords. If it does, that's a trigger-phrase edit CLAUDE.md flags ("Do not silently change SKILL.md trigger phrases"); call it out as a sanctioned additive edit (existing keywords unchanged) so it isn't silent — same class logged 06-19 ~04:18 | cowork |
| 2026-06-19 ~13:24 UTC | prd-to-product | DSB adopt mode v1 | 1 | 3 | refinement | [Subagent review log] output-summary.md should clarify v1's intra-app-consistency/staleness enforcement is prose-only, mechanical hook deferred to v2 — so a user reading the emitted rule knows they gatekeep composition by hand (accepted-and-fixed into file 4) | cc-subagent |
| 2026-06-19 ~13:24 UTC | prd-to-product | DSB adopt mode v1 | 1 | 3 | refinement | [Subagent review log] cluster-0 bundle-detection signal under-specified — spell out filename, grep keyword, scan depth (accepted-and-fixed into file 2: README in root or claude-design*/design-* dir, self-declaration grep, flagged as explicit exception to "default to asking") | cc-subagent |
| 2026-06-19 ~13:24 UTC | prd-to-product | DSB adopt mode v1 | 1 | 3 | refinement | [Subagent review log] decisions.md adopt branch should document WHY it bypasses tokens.css.template, not just that it does — inline cite to D-044/D-008 fidelity trap so a maintainer doesn't re-route tokens through the scale-first template (accepted-and-fixed into file 3) | cc-subagent |
| 2026-06-19 ~13:40 UTC | prd-to-product | DSB adopt mode v1 | 2 | 3 | refinement | Fix-introduced breakage: new file 9 adds a superseding note at the TOP of design-handoff-adoption.md, which shifts the import-rule fence off lines 64-79. Those line numbers are hardcoded in file 5's source citation, verification check 4, the ledger, and Context — all go stale once file 9 lands. Anchor check 4 / file 5 to the fence CONTENT ("# Design adoption — import, don't rebuild"), or sequence the template copy before the doc edit and update every "64-79" citation. Degrades a RED verification gate | cowork |
| 2026-06-19 ~13:40 UTC | prd-to-product | DSB adopt mode v1 | 2 | 2 | refinement | file 9 enumerates "line 1 'Per D-008 the kit builds no … adopt-mode'" as one of three false-after-adopt lines, but line 1 is the title ("# Adopting a Claude Design bundle"); the "Per D-008 … no design-system-bootstrap adopt-mode" claim is on line 3. Lines 49 and 58 are correctly cited. Re-target to line 3 (the fix — top-of-doc superseding note — is unaffected, but the quote/line pairing is wrong) | cowork |
| 2026-06-19 ~13:40 UTC | prd-to-product | DSB adopt mode v1 | 2 | 3 | refinement | Scope note not updated for the inserted file 9: header now says "~11" files but the note still reads "~10 files" and "land the decision log (9-10)" — after insertion the decision log is files 10-11, and file 9 is now the design-handoff-adoption.md doc edit (not decision log). Fix the checkpoint-cut numbering or the implementer may edit the wrong tail | cowork |
| 2026-06-20 ~12:06 UTC | prd-to-product | G-14 provenance banner | 1 | 3 | must-fix | Codification (file-2) says "strip the authoring block, emit the banner verbatim" but never specifies HOW the substitution agent tells them apart or the banner's SOURCE ordering. Both are `<!-- -->` comments; for plain-md the authoring block currently leads (CLAUDE.md.template:1-6, flat-CLAUDE:1-7) and is stripped, for frontmatter the authoring block sits after `---` (ai-shared:7-12, design-heuristics:8+) and is stripped. If banner is placed before the authoring block in source, a "strip leading comment" rule eats the banner; if the discriminator is left implicit the strip is non-deterministic across runs (this repo's #1 tracked failure = bad substitution). decisions.md must state: banner identified by "Generated by the context-engineering skill" text & kept verbatim; authoring block identified by Template:/Purpose:/Use-this-template-when lines & stripped; banner placed immediately AFTER the authoring block (and, for frontmatter, after the closing `---`) | cowork |
| 2026-06-20 ~12:06 UTC | prd-to-product | G-14 provenance banner | 1 | 3 | refinement | Coverage-gap is understated. Ledger frames the only un-fixtured placement as the 5 frontmatter rules, but output-small/ (verified: no `.codex/` dir) also omits codex-config.toml — so the TOML-top placement is a DISTINCT placement rule with no fixture AND no spot-check (verification only spot-checks the frontmatter case). Add a manual dry-run + TOML-parse spot-check for codex-config.toml.template (banner lands at emitted top, file still parses). DECISIONS_ACTIVE/agents-skills-README/4 non-frontmatter modular rules/modular CLAUDE+AGENTS are also absent from output-small but reuse the already-covered md-before-content rule, so lower risk | cowork |
| 2026-06-20 ~12:06 UTC | prd-to-product | G-14 provenance banner | 1 | 3 | refinement | Marker-bump to D-046 implicitly claims D-045 was evaluated for mirroring, but DECISIONS_ACTIVE.md:1 currently reads "through D-044" while D-045 already exists in DECISIONS.md — D-045 is itself unevaluated. Plan says "noting D-045/D-046 evaluated-not-mirrored" without stating what D-045 is. Confirm D-045 genuinely warrants no mirror (read it, state why) rather than sweeping it into the bump — same marker-drift class caught 06-19 ~04:08/~04:14/~12:22 | cowork |
| 2026-06-20 ~12:06 UTC | prd-to-product | G-14 provenance banner | 1 | 3 | refinement | SKILL.md "Annotation conventions in templates" (line 53) lists 3 `<!-- -->` forms (PARAMETERIZE/KEEP-AS-IS/OPTIONAL), all processed/kept; the strippable authoring block is documented nowhere there. Banner is a 4th kept-verbatim form. File-3 adds one line for the banner — also note the authoring-block-strip convention in the same place so a future template author can discover the banner-vs-authoring-block discriminator without reading generator/decisions.md | cowork |
| 2026-06-20 ~12:06 UTC | prd-to-product | G-14 provenance banner | 1 | 3 | refinement | [Subagent review log] Clarify blank-line handling for frontmatter placement: banner on the line directly after `---` (no intervening blank), then a blank line, then H1 (accepted-and-fixed into placement table) | cc-subagent |
| 2026-06-20 ~12:06 UTC | prd-to-product | G-14 provenance banner | 1 | 3 | refinement | [Subagent review log] JSON `"//"` wording should keep the "edit at your own risk" hooks-disarm caution (accepted-and-fixed: file-5 now carries a concrete target string) | cc-subagent |
| 2026-06-20 ~12:06 UTC | prd-to-product | G-14 provenance banner | 1 | 3 | refinement | [Subagent review log] Verify D-046 not consumed during authorship (accepted, no change: re-grep-at-write-time already covers it) | cc-subagent |
| 2026-06-20 ~12:40 UTC | prd-to-product | G-14 provenance banner | 2 | 3 | refinement | Round-1 Must-fix + 3 Should-considers all verified resolved against code (D-045 Reason quoted verbatim; source-order line numbers exact). New: the (a)/(b)/(c) strip codification doesn't specify blank-line consumption around the authoring block. For frontmatter (design-heuristics source: frontmatter 1-7, blank 8, block 9-14, blank 15, H1 16) the emit must hit "banner directly after `---`, no blank between" — which requires stripping the LEADING blank (line 8) along with block 9-14, else emit = `---`/blank/banner. (c) only says strip the block; an implementer stripping only 9-14 leaves a stray blank. Un-fixtured path (no frontmatter fixture), so the ai-shared spot-check is the only backstop — name the blank-consumption rule in decisions.md to save a rework cycle. Plain-md case is fixture-covered, low risk | cowork |
| 2026-06-20 ~13:02 UTC | prd-to-product | Context-lifecycle dogfood | 1 | 2 | must-fix | Ledger says "Next free decision id is D-046 → Confirmed" — FALSE against the merge target. Plan+ledger authored in worktree branch claude/xenodochial-heisenberg-aef7ba, which is 2 commits behind main; on main D-046 is already taken (### D-046 G-14 provenance banner, DECISIONS.md:418, commit 62508dd). Precondition 0a greps only the LOCAL (stale) DECISIONS.md → sees D-046 free → allocates it → second conflicting D-046 on merge. Fix: 0a must check main too (git show main:docs/DECISIONS.md | grep '^### D-0' | tail -1), allocate max(local,main)+1 = D-047, or rebase the worktree onto main before writing; renumber D-046→D-047 throughout | cowork |
| 2026-06-20 ~13:02 UTC | prd-to-product | Context-lifecycle dogfood | 1 | 3 | must-fix | "Append D-046 before the line-418 footer" anchors the DECISIONS.md insertion by line number. On main D-046 already sits ~line 418 and the file is 428 lines; once the branch syncs with main these line numbers shift. Anchor the insertion by footer TEXT (mirroring precondition 0b "by content, not line number"), not "line 418" | cowork |
| 2026-06-20 ~13:02 UTC | prd-to-product | Context-lifecycle dogfood | 1 | 3 | refinement | DECISIONS_ACTIVE.md C-10 marker reads "Reconciled … through D-044"; main carries D-045 (AB-03) and D-046 (G-14) not evaluated in this branch's copy. The ritual's "bump the C-10 marker" must reconcile D-045 + main's D-046 too, not just the new id, or the marker lands stale-on-arrival post-merge — same marker-drift class logged repeatedly (06-19, 06-20 ~12:06) | cowork |
| 2026-06-20 ~13:02 UTC | prd-to-product | Context-lifecycle dogfood | 1 | 3 | refinement | BACKLOG restructure undefined vs existing headers: file has H2 ## In progress (7), ## Backlog (12), ## Done (59); plan adds H3 ### Active (index) / ### Legacy (converting on touch) without saying whether they replace/nest-under/sit-beside the existing H2s or where ## Done fits. No anchor links to these sections exist (verified: grep "BACKLOG.md#" empty; 94 file-level refs only) so no link breakage, but the resulting header tree must be specified for an unambiguous rewrite | cowork |
| 2026-06-20 ~13:02 UTC | prd-to-product | Context-lifecycle dogfood | 1 | 3 | refinement | After-size margin rests on an estimate the phasing can undercut. Full file ≈30,864 tokens; sweeping ~9 RETIRE (~3,800w) + extracting 2 mega-items (~2,100w) leaves ~6 still-fat EXTRACT items in Legacy (crib-adoption/repo-miner/red-team/superpowers×3/harness-batch/animations ≈2,800w). Projected ≈13K tokens — under the 25K cap but not by a wide margin. If verification step 1 trips, the fix is thinning more Legacy items THIS session; flag that the phasing fallback (extract one mega-item) could under-deliver the cap goal, not just the proof goal | cowork |
| 2026-06-20 ~13:02 UTC | prd-to-product | Context-lifecycle dogfood | 1 | 3 | must-fix | [Subagent review log] D-046 not yet allocated; make "verify free before executing" explicit → added precondition 0a (re-grep next-free at write time, renumber if taken) | cc-subagent |
| 2026-06-20 ~13:02 UTC | prd-to-product | Context-lifecycle dogfood | 1 | 3 | must-fix | [Subagent review log] ticket links using bare/relative anchors 404 on archive-move → added link-depth rule (path#anchor, +1 ../ on archive) + re-verify-links step in ritual & verification 3 | cc-subagent |
| 2026-06-20 ~13:02 UTC | prd-to-product | Context-lifecycle dogfood | 1 | 3 | must-fix | [Subagent review log] rewritten Format section contradicts legacy fat entries left in body → split body into ### Active (index) + ### Legacy (converting on touch) with transition note | cc-subagent |
| 2026-06-20 ~13:02 UTC | prd-to-product | Context-lifecycle dogfood | 1 | 3 | refinement | [Subagent review log] phasing fallback underdelivers the proof goal → fallback now requires retro to flag deferred mega-item as next-highest-priority, not parked | cc-subagent |
| 2026-06-20 ~13:02 UTC | prd-to-product | Context-lifecycle dogfood | 1 | 3 | refinement | [Subagent review log] ritual leaves "who moves the ticket" undefined → specified Claude Code does it via git mv in the end-session commit flow; flagged Bash permission for ExitPlanMode | cc-subagent |
| 2026-06-20 ~13:02 UTC | prd-to-product | Context-lifecycle dogfood | 1 | 3 | refinement | [Subagent review log] D-046 should pre-state binding-ness → added "D-046 pre-drafted binding content" block (retirement mandatory, archive-not-delete, thin-on-touch is a rule) | cc-subagent |
| 2026-06-20 ~14:40 UTC | prd-to-product | Context-lifecycle dogfood | 2 | 2 | must-fix | Precondition 0d gates header-collapse on `grep -rn "BACKLOG.md#"` being empty, and the verification ledger claims it was "verified empty pre-session" — re-running it NOW returns trial-ledger.md rows (ledger prose mentions the literal string, incl. a .claude/worktrees/ copy), so the gate is non-empty and the ledger claim is stale-false. Confirmed there are ZERO real markdown anchor links (`grep "](.*BACKLOG.md#"` and `"](.*DECISIONS_ACTIVE.md#"` both empty), so the collapse IS safe — but the literal gate misfires and per 0d's own conditional would wrongly preserve the headers, under-delivering the thin-index goal. Fix 0d to grep link-syntax `](...#` and exclude trial-ledger.md/.claude/worktrees | cowork |
| 2026-06-20 ~14:40 UTC | prd-to-product | Context-lifecycle dogfood | 2 | 3 | refinement | Verification 6 ("clean tree, only named files") will also surface a pre-existing untracked `.claude/settings.local.json` not named in the plan; and recursive `grep -rn`/`wc` from repo root descend into a live `.claude/worktrees/xenodochial-heisenberg-aef7ba/` checkout (double-counts the trial-ledger). Scope verification commands to the main worktree and explicitly ignore settings.local.json so the clean-tree check doesn't false-trip | cowork |
| 2026-06-20 ~14:40 UTC | prd-to-product | Context-lifecycle dogfood | 2 | 3 | refinement | §4 labels build-defaults item 1 a "KEEP-THIN index line (already a one-liner)", but it is a ~150-word multi-sentence entry (qventus post-mortem + brief refs). Splitting the line-10 concatenation edits it, which by the plan's own thin-on-touch rule should convert it. Resolve the contradiction: either keep it verbatim in Active (and don't call it thin) or convert-on-touch — don't leave a fat entry mislabeled thin | cowork |
| 2026-06-20 ~14:40 UTC | prd-to-product | Context-lifecycle dogfood | 2 | 3 | must-fix | [Subagent review log] header restructure under-specified (where does ## Done fit; are old H2s deleted?) → §1 now states explicit resulting H2 tree (Active→Legacy→Done→Format→Cross-references) and deletes old In progress/Backlog H2s | cc-subagent |
| 2026-06-20 ~14:40 UTC | prd-to-product | Context-lifecycle dogfood | 2 | 3 | must-fix | [Subagent review log] foreground the D-ID allocation guard → 0a gains explicit renumber-if-moved guard (max+1 if any D-NNN>D-047 landed) | cc-subagent |
| 2026-06-20 ~14:40 UTC | prd-to-product | Context-lifecycle dogfood | 2 | 3 | must-fix | [Subagent review log] marker reconciliation must name what "everything since" includes (D-045/D-046 possibly unmirrored) → §2 states evaluate-every-id-between-marker-and-max rule; outstanding = D-047 skip + D-048 mirror | cc-subagent |
| 2026-06-20 ~14:40 UTC | prd-to-product | Context-lifecycle dogfood | 2 | 3 | must-fix | [Subagent review log] archive-move link-depth rule needs concrete steps → §2 spells out ../→../../ rewrite, bare-#anchor conversion, test-move-and-follow-one-link before commit | cc-subagent |
| 2026-06-20 ~14:40 UTC | prd-to-product | Context-lifecycle dogfood | 2 | 3 | must-fix | [Subagent review log] extracting furnace-trial risks orphaning the bucket rubric → premise corrected (rubric lives in trial-ledger legend); §4 requires ticket to point to ledger/D-018/D-020/retros and not restate the rubric | cc-subagent |
| 2026-06-20 ~14:40 UTC | prd-to-product | Context-lifecycle dogfood | 2 | 3 | refinement | [Subagent review log] phasing fallback could under-deliver the cap goal → §4 adds hard threshold: if BACKLOG still >~18K tokens after sweep+furnace, extract plan-review this session | cc-subagent |
| 2026-06-20 ~14:40 UTC | prd-to-product | Context-lifecycle dogfood | 2 | 3 | refinement | [Subagent review log] ritual needs git mv Bash permission confirmed → carried into ExitPlanMode allowedPrompts | cc-subagent |
| 2026-06-21 ~02:58 UTC | prd-to-product | Lifecycle port to skill | 1 | 3 | must-fix | output-small is the only full fixture tree and it is FLAT shape (AGENTS.md carries rules inline, no .claude/rules/). Change B edits claude-rules-flat-AGENTS.md.template unconditionally, so output-small/AGENTS.md MUST gain the graduation block — but §F's fixture list names only end-session.md + BACKLOG.md, omitting AGENTS.md. The plan's own verification ("dry-run the flat AGENTS output and grep for the phrase — present after") cannot pass against an unchanged fixture, and the regression diff drifts. Add AGENTS.md to the §F fixture changes | cowork |
| 2026-06-21 ~02:58 UTC | prd-to-product | Lifecycle port to skill | 1 | 3 | refinement | The modular half of change B (session-discipline.md.template) has no committed fixture: examples/ holds only output-small (flat) + output-large/medium-abbreviated.md single-file summaries — no full tree with .claude/rules/session-discipline.md. So "diff the changed templates against examples/output-small" never exercises the modular block; it can only be ad-hoc dry-run. State that explicitly as self-verified, or accept the modular coverage gap | cowork |
| 2026-06-21 ~02:58 UTC | prd-to-product | Lifecycle port to skill | 1 | 3 | refinement | Change B scaffolds the abstract meta-pattern ("a registry read every session, holding a mutable item-set, with no retirement step graduates to thin-index + on-demand parts") into every project. In a freshly scaffolded small project the only such registry is BACKLOG.md. Recommend writing the rule concretely (name BACKLOG.md + its read-cap trigger) rather than importing this repo's internal D-048 meta-vocabulary — abstraction a small project can't act on is the accretion D-013/D-019 resist | cowork |
| 2026-06-21 ~02:58 UTC | prd-to-product | Lifecycle port to skill | 1 | 3 | refinement | §Scope Slice B reads "tickets/ template + inclusion plumbing + graduation emission" but §C/§E corrected the design to have NO inclusion-table row (the README is graduation-emitted at runtime). "inclusion plumbing" is leftover from the pre-correction draft and could mislead CC into adding a row the design forbids — reword Slice B to drop it | cowork |
| 2026-06-21 ~02:58 UTC | prd-to-product | Lifecycle port to skill | 1 | 3 | must-fix | [Subagent review log] Inclusion-table design incompatibility — graduation-emitted is not a defined emission gate (always/rule-shape/boolean only) → resolved to option B, README is runtime-reproduced source content, no table row | cc-subagent |
| 2026-06-21 ~02:58 UTC | prd-to-product | Lifecycle port to skill | 1 | 3 | refinement | [Subagent review log] Graduation-rule placement in flat shape unverified to have a natural session/loading-discipline home → added build-time gate to confirm before writing | cc-subagent |
| 2026-06-21 ~02:58 UTC | prd-to-product | Lifecycle port to skill | 1 | 3 | refinement | [Subagent review log] principles.md grepped but not fully audited for a parallel product-path allowlist → kept the re-grep explicitly in the allowlist verification step | cc-subagent |
| 2026-06-21 ~02:58 UTC | prd-to-product | Lifecycle port to skill | 1 | 3 | refinement | [Subagent review log] D (BACKLOG graduation pointer) also touches the fixture but only end-session.md was named → fixture line now names both end-session.md and BACKLOG.md | cc-subagent |
| 2026-06-21 ~03:30 UTC | prd-to-product | Lifecycle port to skill | 2 | 3 | refinement | The fixture's ## Session discipline section (output-small/AGENTS.md L44+) is hand-ABBREVIATED to terse bullets, not a literal substitution of the flat template's full-prose section — so "dry-run substitution diff matches the fixture" is not byte-exact for AGENTS.md. CC must author the graduation rule in the fixture's condensed bullet style (not paste the template's prose block) and confirm the red/green grep phrase lands in BOTH the prose template and the bulleted fixture. Otherwise minor; R1 Must-fix + all three refinements resolved cleanly | cowork |
| 2026-06-21 ~12:53 UTC | prd-to-product | CF-29 PRD-FORMAT pilot | 1 | 2 | must-fix | Verification step 1 false-REDs on a correct state: FORMAT skeleton built from principles.md:58-71 (14 sections incl. 2 OPTIONAL — Brand and voice, Supporting documents) is diffed against examples/small/PRD.md (12 sections, both optionals correctly absent). "No section in one and missing from the other" RED-fails today; implementer risks "fixing" it by adding optionals to the fixture. Criterion must diff the 12-section always-emitted core and treat OPTIONAL sections as allowed-absent | cowork |
| 2026-06-21 ~12:53 UTC | prd-to-product | CF-29 PRD-FORMAT pilot | 1 | 2 | must-fix | "No behavior change" (step 4 + ledger) is false for NOTES regression test 1: re-read shows test 1 enumerates 11 always-on sections and OMITS Testing decisions, while template:73 ("Always emitted"), principles.md:69, and examples/small/PRD.md:71 all carry it as #12. Three divergent counts in-repo (11/12/14). Pointing test 1 at FORMAT necessarily rewrites what it asserts (a D-029-era backfill gap), so the "only the pointer moved" claim would steer CC to preserve the stale 11-section list and defeat the single-source goal. Reconcile test 1 to FORMAT's 12-core+2-optional list as a deliberate correction | cowork |
| 2026-06-21 ~12:53 UTC | prd-to-product | CF-29 PRD-FORMAT pilot | 1 | 3 | refinement | Position-aware placement (principles.md:19-27) dictates top/middle/bottom section ORDER but is not in the move list (scope item 2 moves only hand-off/style/content-conventions). FORMAT now owning the ordered skeleton while principles independently restates order = two homes for ordering, the exact drift CF-29 kills. Decide: ordering rationale points at FORMAT's skeleton as authority (no re-list) or moves | cowork |
| 2026-06-21 ~12:53 UTC | prd-to-product | CF-29 PRD-FORMAT pilot | 1 | 3 | refinement | Move-not-add grep (step 2) under-enumerates surfaces: ledger bills 5 but the success-test names only a few rule strings. Specifically template PRD.md.template:48 already points at principles "No volatile code locations" — when that body moves to FORMAT the inbound ref must redirect or step-3 cross-ref check fails. Enumerate every shape surface incl. this pointer. (Cross-skill blast radius clean: grep of skills/ shows no context-engineering path-reference to prd-creator's principles sections; hand-off depends on emitted section names, not the file) | cowork |
| 2026-06-21 ~13:18 UTC | prd-to-product | CF-29 PRD-FORMAT pilot | 2 | 3 | must-fix | Runtime-loading gap the move introduces: binding shape rules (sentence-case, no em dashes, section order, the nuanced "no volatile code locations" carve-out) currently live in templates/PRD.md.template KEEP-AS-IS comments — the LOADED artifact at generation (SKILL.md:46 step 6 substitutes against it). Plan thins those to "terse cues" and moves rule bodies to PRD-FORMAT.md at skill root, which Sign-off C frames as a reference doc "alongside principles.md" — and principles.md is explicitly NOT loaded per invocation (SKILL.md:50 "Reference, not boot"). Plan never says step 6 loads FORMAT, so nuanced rules may sit unloaded at generation → "emitted shape unchanged" is false in practice. Same class as G-06 (06-18 ~16:20, rule parked in non-loaded file). Fix: either step 6 explicitly loads FORMAT w/ a must-load pointer (CF-25), or terse cues stay self-sufficient and FORMAT is needed only at diff time. Design fork for Sign-off B | cowork |
| 2026-06-21 ~13:23 UTC | prd-to-product | CF-29 PRD-FORMAT pilot | 3 | 2 | must-fix | r2 fix premise wrong against files: it asserts operative style rules "currently live in the loaded template KEEP-AS-IS comments," but grep of templates/PRD.md.template shows sentence-case / no-em-dashes / no-Oxford / no-colons / AP-style appear NOWHERE in it — they live only in principles.md:82-90 (UNLOADED). Only volatile-code (L48 restatement+pointer) and imperatives/checkable (L29) are actually in the loaded template. So step 4's "every operative style rule must be self-sufficient in the loaded template, survives-only-in-FORMAT = RED" is RED-on-arrival NOW for the 4 style rules — satisfying it means HOISTING them into the template for the first time = new generation behavior, contradicting step 5's "emitted PRD unchanged." Split per-rule: hoist (named behavior change, drop no-change claim for prose style) or leave reference-only (status quo, remove from step-4 must-be-loaded list) | cowork |
| 2026-06-21 ~13:23 UTC | prd-to-product | CF-29 PRD-FORMAT pilot | 3 | 3 | refinement | Steps 2 and 4 contradict on the thin-restatement+pointer pattern: step 2 says template refs "must be a pointer, not a second copy … surviving duplicate body = RED," but step 4 + r2 resolution require the template keep self-sufficient operative rule BODIES. The existing L48 shape (thin operative restatement + pointer to full rule) is exactly what step 4 preserves yet step 2 would flag. Reword step 2 to sanction restatement+pointer, RED only on full duplicated body. Also: the runtime carve-out means each hoisted rule lives in 2 places (template line + FORMAT body) — name FORMAT authoritative-on-conflict + add a no-divergence guard, else the pilot reintroduces the small-scale template-vs-reference drift CF-29 exists to kill | cowork |
| 2026-06-21 ~13:38 UTC | prd-to-product | CF-29 PRD-FORMAT pilot | 4 | 2 | must-fix | r3's Camp A/B split is imprecise: it asserts the house-style rules incl. sentence-case "live only in principles.md:82-90 (unloaded)" because `grep templates/PRD.md.template` found no style strings — but that grep was scoped to the template only. Broader grep shows SKILL.md:55 ("annotated PRD skeleton. Sentence-case headers, H1/H2/H3 only") AND examples/README.md:25 ("sentence-case, no em dashes") both carry sentence-case, and SKILL.md IS loaded every invocation (it's boot; principles:50 marks only principles "not loaded"). So for sentence-case the "fully unloaded today" premise feeding Camp-B fork (iv) is false, and verification step 2's move-grep will surface SKILL.md:55 + README:25 as unaccounted sentence-case surfaces (scope item 4 never reconciles them). Fix: correct the Camp-B claim and enumerate both surfaces as sanctioned thin-mentions (or redirect) in step 2 | cowork |
| 2026-06-21 ~13:38 UTC | prd-to-product | CF-29 PRD-FORMAT pilot | 4 | 1 | refinement | Section-count reconciliation bills "three divergent counts (11/12/14)" but a fourth stale count sits in the same file: NOTES.md:21 ("PRD-creator emits eleven") in the "Context-engineering gap" historical note. Plan corrects only NOTES test 1 (line 50), leaving a contradicting count a few lines above it — incomplete close of the in-file drift the pilot exists to kill. Name it and make an explicit keep-as-dated-historical vs correct call (related: context-engineering/NOTES.md:70 also says "eleven-section shape," out of pilot scope) | cowork |
| 2026-06-21 ~13:38 UTC | prd-to-product | CF-29 PRD-FORMAT pilot | 4 | 3 | refinement | generator/decisions.md is named as the 5th scattered shape surface in the plan's context, but it is NOT in the 5-file touch list (items 1-5) and gets no FORMAT pointer. Its emission table (decisions.md:9-25) carries a partial section enumeration, so it can drift from FORMAT's canonical list. Either confirm it is legitimately out (emission/conditional logic ≠ body-shape, so it stays) and drop it from the "scattered surfaces" billing, or add a pointer | cowork |
| 2026-06-22 ~09:43 CDT | prd-to-product | /mine v1 source-agnostic mining skill | 1 | 3 | refinement | Cap headroom is zero: estimate ceiling (SKILL ~180 + FORMAT ~120 = 300) equals the ≤300 feature cap exactly, so the pre-commit `wc -l` trim path is the *likely* outcome, not a fallback. But a dogfood RED condition is "missing any FORMAT column." Plan says "trim MINE-FORMAT.md to the minimal shapes" without stating the trim removes prose/explanation only and must preserve all four shapes (mined-doc, lens-A crib row, lens-B board row+ticket, soft-source experiment ticket). Name that constraint so the cap-trim can't strip a shape the verification then RED-fails on | cowork |
| 2026-06-22 ~09:43 CDT | prd-to-product | /mine v1 source-agnostic mining skill | 1 | 3 | refinement | Dogfood RED check "git status shows any product/tracker file changed before adopt" / "clone appears in git status" will noise-trip: a pre-existing untracked `.claude/settings.local.json` is already in `git status --short`, and recursive scans can descend into `.claude/worktrees/`. This exact false-trip was logged twice (06-20 rows). Scope the dogfood git checks to tracked product/tracker paths and explicitly ignore settings.local.json / worktrees | cowork |
| 2026-06-22 ~09:43 CDT | prd-to-product | /mine v1 source-agnostic mining skill | 1 | 3 | refinement | Pre-existing sync drift the plan doesn't surface: `tickets/mine.md` frontmatter is `seq: 2` while its BACKLOG row is `Seq 1` — tickets/README requires status/seq to mirror the board row. Low impact (landing flips the ticket to done + archives it, mooting seq), but the landing step should note the ticket is currently out of sync rather than carry seq:2 forward | cowork |
| 2026-06-22 ~09:43 CDT | prd-to-product | /mine v1 source-agnostic mining skill | 1 | 2 | refinement | Lens-A status vocabulary slightly overstated: ledger/MINE-FORMAT spec status enum as `Proposed \| Adopted (→ D-NNN) \| Declined \| Parked`, but the live inline legend in cribs-from-pocock-craft.md:26 declares only three (Proposed · Adopted · Declined); "Parked" is a subsection/bucket added in the 6-lens re-mine, not a row-status value. MINE-FORMAT should match the 3-state row legend (or note Parked is a bucket, not a status) so emitted crib rows slot in cleanly | cowork |
| 2026-06-22 ~09:43 CDT | prd-to-product | /mine v1 source-agnostic mining skill | 1 | 1 | must-fix | [Subagent review log] Line-5 reference had wrong path structure (escaped the project root) → partially accepted; plan-file link actually resolved (plan lives in ~/.claude/plans/, repo link-depth rule doesn't bind it); simplified both refs + added explicit link-depth line for the built skill files | cc-subagent |
| 2026-06-22 ~09:43 CDT | prd-to-product | /mine v1 source-agnostic mining skill | 1 | 3 | must-fix | [Subagent review log] Phase-1 line-count estimate UNVERIFIED, risks the build → accepted; added hard pre-commit gate (`wc -l skills/mine/*.md`, trim if >300, record count, never commit over-cap) | cc-subagent |
| 2026-06-22 ~09:43 CDT | prd-to-product | /mine v1 source-agnostic mining skill | 1 | 3 | must-fix | [Subagent review log] Sign-off option (b) "full skill in one go" silently violates the ≤300 cap without a decision → accepted; reframed as off-the-table unless Rex first logs a cap-override decision | cc-subagent |
| 2026-06-22 ~09:43 CDT | prd-to-product | /mine v1 source-agnostic mining skill | 1 | 3 | must-fix | [Subagent review log] Landing approval gate not named distinctly from the synth-write gate → accepted; engine step 6 now names both (synth-no-Write + propose-and-wait) | cc-subagent |
| 2026-06-22 ~09:43 CDT | prd-to-product | /mine v1 source-agnostic mining skill | 1 | 1 | refinement | [Subagent review log] D-055 reservation provisional → accepted (already covered): re-confirm next-free at write time (furnace 1a) | cc-subagent |
| 2026-06-22 ~09:43 CDT | prd-to-product | /mine v1 source-agnostic mining skill | 1 | 3 | refinement | [Subagent review log] Shallow clone → confusing git state in cache → accepted; storage section notes shallow/detached clones, don't run interactive git there | cc-subagent |
| 2026-06-22 ~09:43 CDT | prd-to-product | /mine v1 source-agnostic mining skill | 1 | 3 | refinement | [Subagent review log] Soft-claim experiment ticket needs an explicit kill-condition format → accepted; MINE-FORMAT now requires a falsifiable Kill-condition field | cc-subagent |
| 2026-06-22 ~09:43 CDT | prd-to-product | /mine v1 source-agnostic mining skill | 1 | 3 | refinement | [Subagent review log] validate-skills.sh timing → accepted; verification runs it before the landing commit | cc-subagent |
| 2026-06-22 ~09:43 CDT | prd-to-product | /mine v1 source-agnostic mining skill | 1 | 3 | refinement | [Subagent review log] Draft the three DECISIONS_ACTIVE mirror lines now → accepted-deferred; binding bits named, exact one-liners drafted at landing against the D-020/D-042 template | cc-subagent |
| 2026-06-22 ~12:38 CDT | prd-to-product | CF-29 DSB DESIGN_SYSTEM-FORMAT | 1 | 2 | must-fix | Plan body (Files-to-touch item 3 + verification step 1) lists "dark-mode token conventions" as an OPTIONAL section the DESIGN_SYSTEM.md.template carries — but the template has NO dark-mode section/heading/OPTIONAL marker (`grep -i dark` on the template hits only two table-cell descriptions; `has_dark_mode`/Q7d gates the block in tokens.css.template per decisions.md:158, not the doc). The plan's own verification ledger names only 3 template OPTIONALs (66/99/189) — body contradicts ledger. Also NOTES.md:31/42 say medium/large DESIGN_SYSTEM.md DO document dark-mode conventions + full-token-listing + accessibility-notes, none exercised by output-small — so a FORMAT verified only vs small under-specifies the doc | cowork |
| 2026-06-22 ~12:38 CDT | prd-to-product | CF-29 DSB DESIGN_SYSTEM-FORMAT | 1 | 3 | refinement | Skeleton conflates OPTIONAL section-headings with OPTIONAL table-rows. Of the named template OPTIONALs only `### A note on the Tailwind path` (line 189) is a heading; secondary-brand (66) and mono-font (99) are inline OPTIONAL table ROWS that will never appear in verification step 1's `grep -nE '^#{1,3} '`. Listing them as allowed-absent skeleton sections is a category error | cowork |
| 2026-06-22 ~12:38 CDT | prd-to-product | CF-29 DSB DESIGN_SYSTEM-FORMAT | 1 | 3 | refinement | Template line-1 instruction is underspecified: DSB template already opens with a 10-line block comment (lines 1-10 Template/Purpose/Canonical source/Related principles), unlike prd-creator's standalone one-line comment. "Add only an authority-pointer comment at line 1 ... touch nothing else" risks clobbering/splitting that block — specify prepend-above-the-block vs add-a-line-inside | cowork |
| 2026-06-22 ~12:38 CDT | prd-to-product | CF-29 DSB DESIGN_SYSTEM-FORMAT | 1 | 2 | refinement | Verification step 4 lists `../docs/DECISIONS.md` as a link to resolve; correct depth from skills/design-system-bootstrap/ root is `../../docs/DECISIONS.md` (confirmed principles.md:143 uses ../../). FORMAT file + verification example must use ../../docs/ | cowork |
| 2026-06-22 ~12:38 CDT | prd-to-product | CF-29 DSB DESIGN_SYSTEM-FORMAT | 1 | 3 | refinement | Verification step 2 ("relocated bodies survive only in FORMAT") can false-RED against step 3: the falsifiable-rationale text overlaps between principles.md (moving) and the template's loaded operative echo at line 24 (staying byte-identical). Scope step 2 to principles.md only and explicitly exempt template lines 22/24 echoes from the no-duplicate check | cowork |
| 2026-06-22 ~12:38 CDT | prd-to-product | CF-29 DSB DESIGN_SYSTEM-FORMAT | 1 | 3 | must-fix | [Subagent review log] Authority comment wording mismatch — proposed comment shorter/different from prd-creator's pattern → accepted-and-fixed; new-file bullet now adapts PRD-FORMAT.md:1 near-verbatim, template pointer pinned to PRD.md.template:1 | cc-subagent |
| 2026-06-22 ~12:38 CDT | prd-to-product | CF-29 DSB DESIGN_SYSTEM-FORMAT | 1 | 3 | must-fix | [Subagent review log] Operative KEEP-AS-IS carve-out absent — verify DSB template operative cues not moved into FORMAT → accepted-and-verified; grepped template, no KEEP-AS-IS comments, operative content (16/18-22/24) named must-stay, verification step 3 checks by line | cc-subagent |
| 2026-06-22 ~12:38 CDT | prd-to-product | CF-29 DSB DESIGN_SYSTEM-FORMAT | 1 | 3 | refinement | [Subagent review log] grep regex precision (H4+) → accepted; `^#{1,3} ` excludes 4-hash, bold table labels aren't headings, step 1 RED condition clarified | cc-subagent |
| 2026-06-22 ~12:38 CDT | prd-to-product | CF-29 DSB DESIGN_SYSTEM-FORMAT | 1 | 3 | refinement | [Subagent review log] Verify D-050 relocation is not a reversal → accepted; principles edit + ledger state rule relocated/preserved verbatim, operative firing untouched | cc-subagent |
| 2026-06-22 ~12:38 CDT | prd-to-product | CF-29 DSB DESIGN_SYSTEM-FORMAT | 1 | 3 | refinement | [Subagent review log] OPTIONAL section coverage — confirm small fixture's actual OPTIONAL state → accepted-and-verified; no OPTIONAL sections in small fixture, allowed-absent confirmed, folded into step 1 | cc-subagent |
| 2026-06-22 ~12:38 CDT | prd-to-product | CF-29 DSB DESIGN_SYSTEM-FORMAT | 1 | 3 | refinement | [Subagent review log] CF-29 roadmap entry update scope — specify exact text not "bump" → accepted-and-fixed; logging bullet now gives exact sync-marker clause, notes Wave 3 table rows don't change | cc-subagent |
| 2026-06-23 ~08:43 CDT | prd-to-product | CF-22 /which-skill family router | 1 | 2 | refinement | Path-B frontmatter spec is internally inconsistent: says "match the sibling shape verified in furnace-plan/mine" AND "Set disable-model-invocation: true per D-034" — but reading both siblings' frontmatter shows NEITHER carries a disable-model-invocation field; they are explicit-invoke via the description's "EXPLICIT-INVOKE ONLY" prefix alone. cribs line 82 also flags "is disable-model-invocation even honored by our harness?" as open. Build should follow the actual proven sibling shape (description-prefix, no field) or knowingly add the field, not claim both | cowork |
| 2026-06-23 ~08:43 CDT | prd-to-product | CF-22 /which-skill family router | 1 | 3 | must-fix | [Subagent review log] Plan claims decision number will be D-059 but does not re-verify at write time; ledger entry should be self-contained → accepted-and-fixed (ledger states the 2026-06-23 grep result as a reading not a guarantee, keeps rule-1a re-grep) | cc-subagent |
| 2026-06-23 ~08:43 CDT | prd-to-product | CF-22 /which-skill family router | 1 | 3 | must-fix | [Subagent review log] Critical A/B/C fork not surfaced prominently — must be presented as the crux decision, not buried under a recommendation → accepted-and-fixed (hoisted "THE CRUX" callout to top of sign-off #1) | cc-subagent |
| 2026-06-23 ~08:43 CDT | prd-to-product | CF-22 /which-skill family router | 1 | 3 | refinement | [Subagent review log] Sharpen the defer trigger; bind to logged/observable evidence not inference → accepted-and-fixed (reworded to "a skill-selection misfire is logged (retro failure-tag)") | cc-subagent |
| 2026-06-23 ~08:43 CDT | prd-to-product | CF-22 /which-skill family router | 1 | 3 | refinement | [Subagent review log] SKILL.md links must be relative (../docs/…), not the plan's display paths → accepted-and-fixed (added links-are-relative note + CF-21 reference-by-invocation-name) | cc-subagent |
| 2026-06-23 ~08:43 CDT | prd-to-product | CF-22 /which-skill family router | 1 | 3 | refinement | [Subagent review log] CLAUDE.md edit is self-mod, must be Rex-approved if path B/C → accepted, no change (plan already gates it via sign-off #3 + conditional sentinel) | cc-subagent |
| 2026-06-23 ~08:43 CDT | prd-to-product | CF-22 /which-skill family router | 1 | 3 | refinement | [Subagent review log] Clarify off-surface note placement / keep frontmatter brief → accepted-and-fixed (off-surface detail + tree in body, description stays brief) | cc-subagent |
| 2026-06-23 ~23:21 UTC | prd-to-product | Pattern-A furnace grounding fix (Check 1b) | 1 | 1 | refinement | [Subagent review log] Edit 5 proposed Claude-Code writing trial-ledger.md, but header line 5 forbids it — author inferred CC wrote the 1a divider without reading the header → accepted-and-fixed (Edit 5 dropped, rerouted to Cowork as S-1). A too-narrow read (header unread): the exact Pattern-A class this plan fixes | cc-subagent |
| 2026-06-23 ~23:21 UTC | prd-to-product | Pattern-A furnace grounding fix (Check 1b) | 1 | 3 | refinement | [Subagent review log] Verify generalized scaffold bullet stands alone as a rule, not a pointer back to "Check 1b" → accepted, already satisfied (no furnace vocabulary; grep step proves absence) | cc-subagent |
| 2026-06-23 ~23:21 UTC | prd-to-product | Pattern-A furnace grounding fix (Check 1b) | 1 | 3 | refinement | New scaffold bullet's embedded failure mode is near-tautological ("a narrow check doesn't establish a broad claim") — restates the rule rather than naming a concrete failure like its sibling wired-up bullet ("unregistered hook … looks fine on disk"); thin against architecture-rule-#2's "every rule cites its failure mode" bar | cowork |
| 2026-06-24 ~13:29 UTC | prd-to-product | no-jargon-leak scan port (CE+DSB) | 1 | 3 | refinement | CE `D-0[0-9]` sanctioned exception is correct only because the scan runs at scaffold-time over freshly-emitted output (fresh DECISIONS.md carries just the template:40 numbering comment, zero real D-NNN). Wording risks a future maintainer narrowing/repurposing it wrong: a mature project's emitted DECISIONS.md with real `### D-001` entries would also match `D-0[0-9]` and must not be read as a leak. Anchor the exception to scaffold-time emitted output, mirroring prd-creator's content-region scoping ("where confirmed IDs are part of the deliverable", decisions.md:106) | cowork |
| 2026-06-24 ~13:29 UTC | prd-to-product | no-jargon-leak scan port (CE+DSB) | 1 | 3 | refinement | Edit 1 says the CE "## What the generator never writes" section spans "~539–557"; start 539 is exact but it actually runs to 562 ("## After writing" is at 563). Insert the new section at 563 (after the never-writes block), not at 557 which lands mid-section. Prose placement instruction is unambiguous; line-end estimate is loose | cowork |
| 2026-06-24 ~13:29 UTC | prd-to-product | no-jargon-leak scan port (CE+DSB) | 1 | 1 | refinement | [Subagent review log] Edit 1 line ref imprecise — never-writes section at decisions.md:539 not 553 → fixed | cc-subagent |
| 2026-06-24 ~13:29 UTC | prd-to-product | no-jargon-leak scan port (CE+DSB) | 1 | 1 | refinement | [Subagent review log] Edit 2 path missing /generator/ → fixed to skills/design-system-bootstrap/generator/decisions.md | cc-subagent |
| 2026-06-24 ~13:29 UTC | prd-to-product | no-jargon-leak scan port (CE+DSB) | 1 | 3 | refinement | [Subagent review log] cluster-8 cross-ref didn't name the step → fixed to "8c. Confirm before writing" (intake.md:300) | cc-subagent |
| 2026-06-24 ~13:29 UTC | prd-to-product | no-jargon-leak scan port (CE+DSB) | 1 | 2 | refinement | [Subagent review log] (clean Opus pass) Edit 1's `D-0[0-9]` sanctioned exception described the generator's own source files, not the emitted output the scan targets; CE fixture has zero D-0[0-9] hits → accepted-and-fixed, re-scoped exception to the emitted decisions-log file (template:40) | cc-subagent |
| 2026-06-24 ~14:05 UTC | prd-to-product | Batch A scaffold port (git-hygiene + cross-agent) | 1 | 2 | must-fix | Items 4 & 5 modular code blocks still show `###` headings despite prose + Subagent-log claiming the fix to `##` was applied; modular session-discipline.md.template is uniformly `##` (no `###`), so a verbatim paste nests the new subsection wrongly. Re-read of the snippet falsifies the "plan revised" claim | cowork |
| 2026-06-24 ~14:05 UTC | prd-to-product | Batch A scaffold port (git-hygiene + cross-agent) | 1 | 3 | refinement | Item 6 (a parity fix) paraphrases the modular source sentence ("identical one"/"which wins" vs modular line 39 "identical function"/"which one wins") — copy verbatim or the shapes still differ | cowork |
| 2026-06-24 ~14:05 UTC | prd-to-product | Batch A scaffold port (git-hygiene + cross-agent) | 1 | 3 | refinement | Items 4 and 6 both edit flat-template line 93 (append failure-mode sentence + insert new ### after); flag for one pass so neither is dropped | cowork |
| 2026-06-24 ~14:05 UTC | prd-to-product | Batch A scaffold port (git-hygiene + cross-agent) | 1 | 3 | refinement | Item-2 worktree-cure clause is a no-op/dead-weight for visual-gate projects (same file forbids worktrees there); "where this project allows them" neutralizes it but points to a restricting section — consider suppressing under the visual-gate conditional | cowork |
| 2026-06-24 ~14:05 UTC | prd-to-product | Batch A scaffold port (git-hygiene + cross-agent) | 1 | 3 | refinement | [Subagent review log] Modular insertion heading level ### → ## (modular template is level-2 throughout; ### would nest under "Read before you write") → accepted-and-fixed in prose | cc-subagent |
| 2026-06-24 ~14:05 UTC | prd-to-product | Batch A scaffold port (git-hygiene + cross-agent) | 1 | 3 | refinement | [Subagent review log] Second flat/modular parity gap exists (autonomy failure-mode sentence, modular line 47) → declined for Batch A scope, logged as fresh port-debt | cc-subagent |
| 2026-06-24 ~14:20 UTC | prd-to-product | Batch A scaffold port (git-hygiene + cross-agent) | 2 | 3 | refinement | Internal contradiction on S-1 status: body says "RESOLVED — Rex chose Option A" + "NO UNRESOLVED DECISIONS", but trailing Cowork-review-pass bullet still says "Open decision S-1 … Put to Rex via AskUserQuestion before final ExitPlanMode". Drop the stale line so CC doesn't re-ask | cowork |
| 2026-06-24 ~14:20 UTC | prd-to-product | Batch A scaffold port (git-hygiene + cross-agent) | 2 | 3 | refinement | Item 5 has no insertion anchor for either shape while item 4 specifies exact lines; name placement (adjacent to the reload-co-edited subsection) for parity | cowork |
