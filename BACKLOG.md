# Backlog — prd-to-product

Single surface for work that isn't done. Mid-session deferrals, V2 candidates, watch items, and open investigations all land here.

Read this at session start alongside the most recent retro in `docs/retros/`.

## In progress

- **Build-defaults pilot item 1 — still awaiting validation; qventus ruled out.** Shipped in commit `0afaa17` (2026-05-11). Phase 1 of scaffolded ROADMAPs now derives a deploy-shell from `deploy_target`. **qventus post-mortem (2026-06-08) confirmed qventus cannot validate it** — no `deploy_target`, no deploy-shell in any phase, and its scaffold was *authored to exclude* deployment (git-and-deploy rule + PRD non-goal + Phase-1 export-criterion all hardcode "the file is the product, done = pushed"). The caveat was correct. **Needs a project whose PRD treats a live URL as the deliverable.** Promote to Done when such a project exercises the deploy-shell. Post-mortem: [`docs/retros/2026-06-08-qventus-post-mortem.md`](docs/retros/2026-06-08-qventus-post-mortem.md). Brief: [`docs/build-defaults-brief.md`](docs/build-defaults-brief.md).
- **Skill consolidation: BACKLOG.md scaffolded for new projects.** Mirror the docs-audit pattern (commit `afb2964`) back into `context-engineering`: scaffold a `BACKLOG.md` at root, delete `PARKING_LOT.md.template` and `FUTURE.md.template`, patch ROADMAP terminal phase, "Where to look" tables, session-start command. **Still unvalidated** — qventus uses the old `ROADMAP.md` + `PARKING_LOT.md` shape, so it can't test the consolidation; this needs a project scaffolded *after* the change ships, or a deliberate decision to ship on this repo's own dog-food evidence. ~150–200 lines net change.
- **HTML-over-markdown investigation — not started.** Active per D-001 in [`docs/DECISIONS_ACTIVE.md`](docs/DECISIONS_ACTIVE.md). Parallel-eligible. Brief: [`docs/html-over-markdown-brief.md`](docs/html-over-markdown-brief.md).

## Backlog

- **Validation finding: skill chain doesn't auto-compose; mental model mismatch + misleading repo name.** First observation: 2026-05-11 Squirreled validation. User invoked `/context-engineering` with a working brief, expecting the chain (prd-creator → context-engineering → design-system-bootstrap) to run as one orchestrated flow. The current architecture has three separate skills that hand off manually — composition is the user's responsibility. Result: user skipped prd-creator entirely. context-engineering gracefully accepted the brief in place of a PRD, but the resulting PRD will be thinner than a prd-creator interview would produce. *(2026-06-08 caveat: the Squirreled repo on disk shows the opposite — prd-creator ran, context-engineering didn't — so this "skipped" account is from a different/earlier attempt and was not reconciled. The finding stands regardless: it rests on the unenforced-handoff problem, which the disk confirms.)* **Two connected issues:** (1) sophisticated user mental model assumes one command runs the chain; (2) the repo name `prd-to-product` implies you start with a PRD when the actual starting point is a rough idea / brief — a more accurate name is `idea-to-product`. The rename reinforces the architectural fix: `/idea-to-product` is the natural name for a meta-skill that orchestrates. **Three fix candidates:** (A) build `/idea-to-product` meta-skill that orchestrates the chain; (B) smart routing in context-engineering cluster 0 — detect brief-vs-PRD and offer to call prd-creator first; (C) cheapest — rename the repo to `idea-to-product` + better cluster-0 question + README. Recommend C as first move, escalate to B if C doesn't solve, escalate to A if multi-step orchestration earns its keep. Rename cost: ~30 minutes (GitHub auto-redirects, ~15 in-repo find/replace, no symlink breakage). **Ready (moratorium lifted 2026-06-08):** Squirreled stalled at the PRD → context-engineering handoff, so the experiment that justified deferral never happened — and that stall *is* the strongest evidence for fix-candidate A. Worth its own iteration session. **qventus post-mortem (2026-06-08) tempers candidate A, does not kill it:** qventus ran prd-creator → context-engineering as smooth manual hand-offs, then *correctly declined* design-system-bootstrap because a complete design system already existed — a naive auto-running orchestrator would have clobbered the real tokens. If A is built it MUST branch "skip bootstrap if a design system already exists." See [`docs/retros/2026-06-08-qventus-post-mortem.md`](docs/retros/2026-06-08-qventus-post-mortem.md) §3.
- **Validation finding: skills should auto-detect input, not require explicit attachment.** First observation: 2026-05-11 Squirreled validation. Current behavior: user must explicitly attach the brief with `@/path/to/brief.md` syntax when invoking `/prd-creator` or `/context-engineering`. User expectation: open the project folder, run `/prd-creator`, and have the skill find the brief in the working directory automatically. If found → confirm with user ("I see `docs/brief.md` — is this what you want to work from?"). If multiple candidates → list and ask which to use. If none → ask user whether to paste content, type from scratch, or proceed without source material. **Where to look:** project root, `docs/`, common filenames (`BRIEF.md`, `brief.md`, `*-brief.md`, `IDEA.md`). Connects to the chain-orchestration finding above (same root cause: skills require explicit input plumbing the user shouldn't have to think about). Fix surface: cluster 0 in both `prd-creator` and `context-engineering`. Likely also applies to `design-system-bootstrap` for brand assets. Ready to fix (moratorium lifted 2026-06-08: the Squirreled experiment stalled at the PRD handoff and never ran context-engineering, so "don't change the skill mid-experiment" no longer applies).
- **Fast-follow: propagate the prd-creator vocab-leak + temporal fixes to the sibling skills.** The 2026-06-09 prd-creator pass (D-010, [retro](docs/retros/2026-06-09-prd-creator-intake-fix.md)) resolved three findings there — "ask anyway" overcorrection, temporal hallucination, internal-vocabulary leak — plus the missing `## Gotchas` section. The vocab-leak ("Cluster N"/"0a/0b" in user-facing copy) and temporal-claim patterns are confirmed present in `context-engineering/generator/intake.md` and `design-system-bootstrap/generator/intake.md` (grepped: same `## Cluster N` headers, `0a/0b` notation, and "always ask Q0a" language). Apply the same scrub + no-temporal-claims guardrail there, verifying with the broadened category grep (`[Cc]luster [0-9]` zero in user-facing copy; no provenance tags). **Do NOT propagate the ask-anyway→draft-and-present half blindly:** context-engineering's Q0a *is* the source-material question itself, which stays — only the downstream-cluster behavior would change, and it has no validation evidence yet in those skills. Promotion trigger: any session touching those intake files, or a real run that surfaces the leak there. ~80-120 lines across the two skills + their examples.
- **Verify `design-system-bootstrap` actually triggers and produces usable output.** Skill is built and symlinked but no retro covers its validation in this repo's discipline. Promote to In progress with a real test project. **qventus could NOT validate it (post-mortem 2026-06-08):** the one real project where it could have run *declined* it — qventus already had a complete hand-authored `--primary` token system from a Claude Design handoff, and the skill explicitly does not migrate an existing system. The reference layer that *was* hand-integrated there was later killed (D-033) once the static rewrite consumed none of it — evidence against a *separate* design-system layer for single-artifact prototypes, but the skill's from-scratch path remains untested. **Needs a bootstrap-from-nothing project.** See [`docs/retros/2026-06-08-qventus-post-mortem.md`](docs/retros/2026-06-08-qventus-post-mortem.md) §2.
- **Build-defaults item 5 (check/test pre-commit, prose-only).** Pilot pending item 1's real-project evidence. Brief: [`docs/build-defaults-brief.md`](docs/build-defaults-brief.md).
- **Build-defaults item 6 (defer abstraction).** Pilot pending items 1, 5.
- **Build-defaults item 2 (vertical slice).** Pilot pending items 1, 5, 6.
- **Build-defaults item 3 (test-first for logic).** Pilot pending earlier items.
- **Hook architecture defect: unconditional scripts scoped only by a non-standard `"if"` matcher field (found + partially fixed 2026-06-08).** Root cause: every emitted hook script (`block-env-commit`, `block-deploy-cli`, `block-worktree`) is an unconditional `echo`+`exit 2`, relying entirely on `settings.json` `"matcher": "Bash"` + a `"if": "Bash(…)"` field to scope it. `"if"` is **not a standard Claude Code hook field** (the official pattern: matcher selects the tool, the *script* reads the JSON payload on stdin and decides). Observed: the env hook fired on a benign read-only command (`git rev-list … && ls …` — no `git add`, no `.env`) and blocked it, while plain `ls` passed. **DONE this session:** `block-env-commit.sh` rewritten to inspect stdin and `exit 2` only on a real `git add/stage … .env` — fixed in the template, this repo's live copy, and `examples/output-small/`; verified with 4 mock payloads (real `.env` staging blocks; benign compound, plain `ls`, normal-file add all pass). Known limitation left in place: doesn't catch bulk `git add .`/`-A` (`.gitignore` is the backstop). **STILL OPEN:** (1) apply the same stdin-inspection fix to `block-deploy-cli.sh` + `block-worktree.sh` (same unconditional-script defect); (2) decide whether the `"if"` field is honored by the target harness at all — if not, the matcher/`if` convention in [`claude-settings.json.template`](skills/context-engineering/templates/claude-settings.json.template) is misleading and should be dropped in favor of script-side scoping. Verify the remaining script fixes by live-firing per the validation contract.
- **`stack_summary_one_line` row missing for `stack=other + deploy_target=none`.** Decisions table in [`skills/context-engineering/generator/decisions.md`](skills/context-engineering/generator/decisions.md) covers Next/React/Node/Python permutations but not `other + none`. One observed instance (Phase 2 dog-food). Fix on second instance per continuous-mode discipline.
- **Skill ecosystem gaps (Thariq 9-category framework).** Eight uncovered categories: Library/API Reference, Product Verification, Data Fetching, Business Process Automation, Code Quality/Review, CI/CD, Runbooks, Infrastructure Ops. Promote to In progress only when a real failure surfaces. Existing three skills all fall in Code Scaffolding.
- **Sibling skill: `red-team` (adversarial review) — ready to promote to In progress.** New skill that takes any document — PRD, scaffolded ROADMAP, design rationale, brief, brand statement — and returns critique. Sibling to prd-creator / context-engineering / design-system-bootstrap rather than a feature inside any of them, because the capability is generic across documents and each existing skill should stay single-purpose. **First real-failure evidence landed 2026-05-11 Squirreled validation:** prd-creator collected answers but didn't pressure-test the V1 plan. User reached for `/brainstorm-ideas-existing` mid-interview to fill the gap. The brainstorm caught a load-bearing differentiator slip ("forced interpretation" → "synthesis quality") that prd-creator missed entirely, plus structural V1 issues (mobile capture, vector embeddings, synthesis output shape, cold-start story). This is no longer speculation — the chain has a hole at "critique before locking in." Likely fix: build `/red-team` OR fold a critique cluster into prd-creator's late flow before cluster 5 (decisions consolidation). Lean toward the sibling skill for the same single-purpose reasons originally cited. Source: 2026-05-11 ideation conversation (#15 Creative Red Team + #18 Narrative Gap Finder) confirmed by 2026-05-11 Squirreled validation.
- **Agent-process & context-harness upgrades — ready, no blocker.** Source: Séance (iOS) session hardening the agent-process layer; full reasoning in [`docs/agent-process-brief.md`](docs/agent-process-brief.md). Five proposed additions to `context-engineering`, in value order: (1) scaffold `/end-session` bookend command + upgrade retro convention to timestamped H1 + session-of-day (fixes same-day "not done this session" misread) — **DONE 2026-06-08**: retro convention (timestamped-H1 + session-of-day + before-commit-bundle + non-trivial-only) formalized in the retro README template/example/this repo, and the `/end-session` bookend command built (template + example + this repo's copy, registered in the generator manifest, cross-referenced from session-discipline + principles). Pairs with the session-start git-history selection fix shipped same day; (2) seed `permissions.allow` in `claude-settings.json.template` from the already-captured `{{install/dev/check/test_cmd}}` + a universal-safe read-only core, defer the rest to `fewer-permission-prompts` — **DONE 2026-06-08** (template + example seeded; drop-if-none + emit-without-hooks rules in `generator/decisions.md`; **this repo's own `.claude/settings.json` still pending** — the self-edit that widens the agent's permission allowlist was classifier-blocked, needs explicit user OK); (3) consolidate the scattered autonomy rules into one "run to done" charter with explicit precedence — *scope limits are the outer gate, run-to-done governs inside them* — plus a cluster-5 interview question for the gated-surface default split — **DONE 2026-06-08** (Autonomy section added to `session-discipline.md.template` + flat-CLAUDE + example + this repo; Q27b + `autonomy_gate_override` added to intake/decisions; visual-gate clause is OPTIONAL on `uses_visual_confirmation_gate`, verified one gated bullet renders per gate value); (4) **reconcile the memory model** — rewrite `principles.md:19–25` and add an AGENTS.md "where facts live" note to one rule: *repo (AGENTS.md/docs) = anything that survives a tool switch (Codex/Cursor can't read Claude's machine-local auto-memory); auto-memory = Claude-local scratch only* — **DONE 2026-06-08** (principles reframed to survives-a-tool-switch; "Where facts live" section added to AGENTS.md.template + flat-CLAUDE + example + this repo); (5) harness-condition the `session-start` AGENTS.md read on `rule_shape` (skip in flat shape where rules auto-load via CLAUDE.md) — **DONE 2026-06-08**: `<!-- OPTIONAL: agents_read_step -->` added to [`session-start.md.template`](skills/context-engineering/templates/claude-commands/session-start.md.template) gated on `rule_shape == "modular"`; flat-shape `examples/output-small` regenerated (AGENTS.md step dropped, list renumbered 1–3); rule documented in [`generator/decisions.md`](skills/context-engineering/generator/decisions.md) "session-start AGENTS.md read is rule-shape-conditional"; dry-run flat substitution diffs clean against the example, modular unchanged. **This repo's own `.claude/commands/session-start.md` still pending** — it's flat shape and still reads AGENTS.md at step 1; dropping it is a self-edit of agent startup config, classifier-gated like the group-2 settings.json edit, needs explicit user OK. **Groups 1–5 shipped 2026-06-08** (group 5's in-repo dog-food self-edit is the only loose end). Connects to the on-demand-hooks open decision below and the BACKLOG-scaffolding in-progress item.
- **Skill injection by project type — domain-layer companion to the agent-process item above. Not started; design question must be resolved first.** Source: [`/Users/rexc/Sites/seance/docs/references/skill-injection-by-project-type.md`](file:///Users/rexc/Sites/seance/docs/references/skill-injection-by-project-type.md) (reviewed 2026-06-08). Where the item above is the *process* layer (always-on, stack-independent — `/session-start`, `/end-session`, hooks, permissions), this is the *domain* layer: teach `context-engineering` to map a new project's **stack + primary output** → a curated skill shortlist, install a tiny **active** set into `.claude/skills/`, **park** the rest in `docs/references/staged-skills/`, and **promote per phase** via ROADMAP notes. Generalizes what Séance did by hand (its `staged-skills/README.md` + per-phase ROADMAP promotion notes are the working reference implementation). Capability is greenfield on top of the one existing hook — Q27a `external_skill_references` ([`intake.md:215`](skills/context-engineering/generator/intake.md)) only cross-links *already-installed* global skills; there is no stack→skill map, no manifest, no active/parked/promote concept. **Blocking design question (resolve before building):** the doc's promote-mechanic assumes you *vendor* repo files and `cp -R` them into `.claude/skills/`, but most candidate skills now arrive as **marketplace plugins** (`anthropic-skills:*`, `design:*`, `data:*`, etc. — ~70 plugin descriptions load every session, the exact context-cost the doc warns about, observed live 2026-06-08). For plugin-sourced skills "active vs parked" is enable/disable, not a filesystem move, and may not be per-project controllable at all. The manifest schema anticipates this with a `source` field (`plugin | marketplace | repo-vendored | url`) but the §4 flow only solves the vendored case. **Don't build until the source-dependent promotion mechanic is designed** — otherwise it ships solving one source. **Scope when it does land:** large (>300-line feature gate) + new generated-artifact type (architecture decision) — needs explicit scope confirmation and likely phasing. For *this* repo specifically the domain layer is a near-no-op (skill-building project → active `skill-creator`, parked `mcp-builder`, both already present as plugins). Defer to `find-skills` for the long tail (doc §6).
- **Broader Appendix-A `/session-start` rewrite — easy, low value, do only if session-start bloat bites.** Group 5 (shipped 2026-06-08) fixed one redundant read (AGENTS.md in flat shape). `agent-process-brief.md` §1.1 / Appendix A proposed the fuller redesign we only partially did: restructure the command from "read 3–4 files every time" into tiers — don't re-read *anything* that auto-loads; **always** read just the latest retro (carries the handoff); read ROADMAP + `DECISIONS_ACTIVE` **only when resuming feature work**, skip for a trivial one-off. Pure markdown template rewrite, target wording already exists in Appendix A; ~3 files + a gated self-edit to this repo's own copy; ≈half a session. **Why deferred, not done:** group 5 already captured the high-value slice; the rest is marginal token savings traded against asking the agent to judge "am I resuming feature work," which can misfire. Promote only if `/session-start` weight becomes a real problem.
- **No modular-shape example output tree for regression — medium effort + ongoing tax; build only if modular work picks up.** The verification contract (CLAUDE.md "Verification before claiming done") diffs dry-run output against [`examples/output-small/`](skills/context-engineering/examples/output-small/) — but that's the *only* example and it's **flat** shape. There is no **modular** example (the shape where rules live in `.claude/rules/*.md`, AGENTS.md is canonical, CLAUDE.md is `@AGENTS.md`). Consequence, seen live in group 5: changes to the modular path can't be diff-verified, only inspected. Closing it = build a committed `examples/output-modular/` from a representative modular input (e.g. tokens-plus-linter design, or ≥1 AI surface), hand-rendering the full tree correctly (AGENTS.md + one-line CLAUDE.md + several `.claude/rules/*.md` + docs + commands + settings + hooks). **Cost:** the baseline must be trustworthy (a wrong baseline is worse than none), and every future template change then has to update *two* trees. **Why deferred:** flat is the common case, modular is rarer, and one good baseline beats two half-maintained ones. Promote when modular-affecting changes become routine. **Council caveat (2026-06-08, [`council-report-2026-06-08.html`](docs/council/council-report-2026-06-08.html)):** do NOT build this as a golden-tree *byte-diff*. Generation is LLM-driven and non-deterministic — a byte-diff fires false positives on legitimate run-to-run variation and trains the operator to ignore it (the opposite of the trust it's meant to protect). Build it as **structural/invariant assertions** instead: required files exist, required sections present, cross-references resolve, OPTIONAL gates evaluated correctly. Assert on structure, not exact bytes.
- **Invariant/semantic output checks — council's "a day" layer. (Write-guard prose AND enforcement hook are DONE.)** Source: LLM Council on improving the kit ([`council-report-2026-06-08.html`](docs/council/council-report-2026-06-08.html) / [`council-transcript-2026-06-08.md`](docs/council/council-transcript-2026-06-08.md)); decisions **D-005** (prose) + **D-006** (hook). **DONE 2026-06-08:** the non-destructive write guard ships as prose across all three skills AND is **hook-enforced** by [`hooks/write-guard.sh`](hooks/write-guard.sh) (global PreToolUse `Write|Edit|MultiEdit`, interactive-`ask` / headless-`deny`, hook-maintained run-owned set; 15/15 unit tests + true end-to-end verified — see **D-006** and [`docs/retros/2026-06-08-write-guard-hook.md`](docs/retros/2026-06-08-write-guard-hook.md)). The qventus clobber class is now **enforced (interactive) / refused (headless)**, not merely mitigated. **STILL OPEN — (b) invariant/semantic checks:** no-jargon-leak (grep "Cluster 0"/"D-NNN" against emitted copy — the prd-creator vocabulary-leak finding), provenance-grounded (every cited fact traces to a given input — the temporal-hallucination finding). **`/idea-to-product` orchestrator-gate update:** the gate is **CLEARED for the clobber-safety class only** — headless runs are *update-incapable* on pre-existing files (they `deny`), so an unattended orchestrator is non-destructive *because* it cannot modify existing files, NOT because its full job runs unattended (D-006 headless ceiling). Two residuals before an orchestrator: the hook is installed only on the operator's machine (not distribution-portable — a shipped plugin would install it at plugin-install time), and a generator that forgets to arm has no enforcement (prose backstop).
- **Token-adopt command for Claude Design bundles — deferred to bundle #2 (Rule of Two).** Per [D-008](docs/DECISIONS.md), the kit builds no adopt-skill / no DSB adopt-mode / no command now: Claude Design bundles stay authoritative, tokens reach a real product via a one-time product-side `cp`. The fidelity test ([`docs/design-handoff-fidelity-test.md`](docs/design-handoff-fidelity-test.md)) showed values are lossless but structure is non-isomorphic to the scale-first template — at N=1 you can't separate a shipped theme from tweak-tool scratch, so any encoded schema is unvalidated. Baseline + manual playbook + bundle ledger recorded in [`docs/design-handoff-adoption.md`](docs/design-handoff-adoption.md). **Promotion trigger:** a 2nd real Claude Design bundle arrives — fill the ledger's second row, re-derive against row 1, and decide whether a token-adopt command earns its keep (defaults to CSS-vars, never Tailwind). Source: [audit council](docs/council/council-report-2026-06-09-audit.html) + [retro](docs/retros/2026-06-09-claude-design-adopt-council.md).
- **Candidate future products parked for exploration.** Three product briefs captured in [`docs/product-briefs/`](docs/product-briefs/) for future PRD development: AI Research Synthesizer, Anti-Generic Machine, Design Process Black Box Recorder. The Black Box Recorder is the most ambitious — multi-system passive-capture product, parked deliberately as a long-horizon idea. None are scoped as next-build; revisit after Taste Builder validation completes.
- **Open decisions from prior roadmap.**
  - Should `prd-creator` and `design-system-bootstrap` switch to HTML output? Gated on HTML investigation above.
  - Verification skill: build, defer, or skip? Decide when continuous-mode failures pile up.
  - When `agent teams` moves from experimental to standard, what changes for the skill? Watch upstream Claude Code releases.
  - Should there be on-demand hook scaffolds in the skill? Pending a real failure mode (e.g., user repeatedly hits a moment where a scoped `/careful` would have helped).

## Done

See [`docs/retros/`](docs/retros/) for the session-by-session record. Skill-build Phases 1–4 (2026-05-10) and continuous-mode sessions (2026-05-10 → 2026-05-11) are recorded there.

## Format

Mid-session deferrals: log here immediately with one line on what + one line on what would promote it to In progress. Resolved items move to a retro, not back to this file.

In-progress entries name a promotion criterion ("when X happens, this is done"). Backlog entries name a promotion trigger ("when X observed, move to In progress").

## Cross-references

- Project rules: [`CLAUDE.md`](CLAUDE.md).
- Active decisions: [`docs/DECISIONS_ACTIVE.md`](docs/DECISIONS_ACTIVE.md).
- Decisions log: [`docs/DECISIONS.md`](docs/DECISIONS.md).
- Session retros: [`docs/retros/`](docs/retros/).
