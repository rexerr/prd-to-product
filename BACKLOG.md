# Backlog — prd-to-product

Single surface for work that isn't done. Mid-session deferrals, V2 candidates, watch items, and open investigations all land here.

Read this at session start alongside the most recent retro in `docs/retros/`.

## In progress

- **Furnace-plan trial — active experiment, Cowork-graded.** Versioned skill at `skills/furnace-plan/` (symlinked into `~/.claude/skills/`; explicit-invoke) born from the 83-session plan-review audit. *(Relocated into the repo 2026-06-17 per [D-020](docs/DECISIONS.md); cross-project capture is unchanged — the global symlink path keeps Cowork capturing every project's plans, so the "local-only non-starter" reasoning below is about plan **origin**, not file location.)* It self-applies a verification preflight (audit themes T1/T2/T6 + a droppable T4 check) before `ExitPlanMode` and appends a `## Verification ledger` to the plan. **Premise correction that shaped it:** Rex catches *none* of these errors himself — every revision in the audit was a Cowork plan-review catch he pasted in — so the furnace is a *pre-filter*, never a replacement; Cowork stays the authoritative reviewer + the only valid oracle (Rex can't grade it). Paired adjustment drafted for the Cowork `plan-review` skill: attack the ledger (catch hollow "verified" entries), reallocate to judgment calls, stay full-scope ("faster, never narrower") — **applied in Cowork 2026-06-13; full loop now wired.** **How to grade — classify each Cowork catch into three buckets, don't just count (refined 2026-06-14 from a creator-vs-checker discussion of Addy Osmani's loop-engineering piece; rides the existing Cowork round-trip, no extra work):** the furnace preflight is a *forcing function* (Check 1: cite every codebase claim to a read run *this session*), which structurally defeats one sub-type of the dominant assert-without-verify class but not the other — so a raw Must-fix count can't read the result. Bucket what Cowork still catches: **(1) no-read-behind-it** — a count or "X is missing" asserted from memory/filename that Check 1's enumeration *should* have caught → forcing function underperforming → **prompt problem, tighten the preflight, don't kill**; **(2) read-behind-it-but-wrong / unfalsifiable inference** — a claim the author believed verified that only a fresh context caught (archetype: the 06-13 Cowork-causal-loop miss — a provenance claim *no* codebase read could establish, so it never got a ledger row and Check 1 sailed past) → self-review *structurally cannot* reach it → **the distinct-verifier-subagent case, not a kill**; **(3) genuine judgment/scope call** — neither layer's job, correctly Cowork+Rex's (the skill already routes these out). **Promotion trigger:** if catches collapse to mostly bucket 3, the mechanical layer has earned its keep — consider promoting from invoke-skill to an `ExitPlanMode` hook (council said earn the hook only after the content proves out — needs live-fire testing). **Kill / branch trigger:** an unchanged catch rate is ambiguous until classified. Mostly **bucket 1** → preflight isn't being executed; a hook would just automate a half-working layer (fix the prompt). Mostly **bucket 2** → the blind spot is real and a planning prompt can't reach it, but the fix is a fresh-context verifier *fed the ledger* (the ledger scopes it cheaply — run one plan through such a verifier as the tiebreaker), **not** a kill. Mostly **bucket 3 with no count drop** is the only true execution-stage-kill case the original live-council caveat named. Evidence + verdict: [`research/feedback-extracts/PATTERNS.md`](research/feedback-extracts/PATTERNS.md), [`research/feedback-extracts/furnace-draft.md`](research/feedback-extracts/furnace-draft.md), [`docs/council/council-report-2026-06-12-furnace.html`](docs/council/council-report-2026-06-12-furnace.html). **Bucket scorecard centralized 2026-06-16; relocated into the repo 2026-06-17 ([D-020](docs/DECISIONS.md)).** Bucket data was previously scattered across retros and never tallied; now a single global ledger, versioned in-repo at `skills/furnace-plan/trial-ledger.md` and reachable from any project via the `~/.claude/skills/furnace-plan/` symlink. **Writer = Cowork's `/plan-review` skill** (edited to append one row per finding per round: `Date(timestamp) | Project | Plan | Round | Bucket | Severity | What Cowork caught`), so capture is automatic across *every* project Cowork reviews — not just this repo, whose plans aren't representative (the bias that made local-only a non-starter). The furnace never writes it; the **outcome column was dropped** (the bucket distribution is the whole signal, and outcome would've created a cross-project retro-discipline gap). **Severity is its own column** because bucket-3 spans refinement↔must-fix (06-15 R1's over-broad trigger was a bucket-3 *must-fix*) — and "mostly bucket-3 *must-fixes*" argues *against* the hook, not for it, refining the promotion read. The legend carries a self-contained audit rubric (promotion/kill/tighten triggers), so **no audit skill** — point any Claude at the file (Rule of Two; build a `/audit-ledger` command only if auditing becomes routine). 8 rows backfilled from the 06-14 / 06-15 retros. **Live-verified (skip path):** Cowork loaded the edited skill and correctly declined to log a non-furnace seance plan; a follow-up edit silenced the "nothing to log" footer so non-furnace reviews stay clean. **Still pending:** the positive path — a furnace plan with a `## Verification ledger` actually producing a row — fires on the next real `/furnace-plan` → Cowork run. Retro: [`docs/retros/2026-06-16-furnace-trial-ledger.md`](docs/retros/2026-06-16-furnace-trial-ledger.md).- **Build-defaults pilot item 1 — still awaiting validation; qventus ruled out.** Shipped in commit `0afaa17` (2026-05-11). Phase 1 of the scaffolded `BACKLOG.md` Build plan now derives a deploy-shell from `deploy_target`. **qventus post-mortem (2026-06-08) confirmed qventus cannot validate it** — no `deploy_target`, no deploy-shell in any phase, and its scaffold was *authored to exclude* deployment (git-and-deploy rule + PRD non-goal + Phase-1 export-criterion all hardcode "the file is the product, done = pushed"). The caveat was correct. **Needs a project whose PRD treats a live URL as the deliverable.** Promote to Done when such a project exercises the deploy-shell. Post-mortem: [`docs/retros/2026-06-08-qventus-post-mortem.md`](docs/retros/2026-06-08-qventus-post-mortem.md). Brief: [`docs/build-defaults-brief.md`](docs/build-defaults-brief.md).

## Backlog

- **README install loop symlinks `context-engineering-audit`, contradicting D-019 (low priority).** The generic `for skill in skills/*/` loop in `README.md` (Install) symlinks *every* `skills/*/` into `~/.claude/skills/` for a fresh cloner — including `context-engineering-audit`, which [D-019](docs/DECISIONS.md) says should **not** be global (it's a design record, missing the field-0 gate). This predates the furnace migration and doesn't affect Rex's machine (where c-e-a is already un-symlinked). Surfaced 2026-06-17 during the furnace-plan migration (D-020); the README note there was scoped to mention only `furnace-plan` to avoid worsening it. **Fix when touched:** either exclude `context-engineering-audit` from the loop (e.g. an explicit skip, or move it out of `skills/`) or add a D-019 caveat in the Install section. **Promote to In progress when** someone actually clones the repo fresh and the stray symlink causes confusion.
- **Brownfield context-drift audit — 3 PILOTS DONE; `/audit-context` skill DECLINED 2026-06-13 ([D-013](docs/DECISIONS.md)). Fix old projects by hand from the audit docs; once current, they're current.** The need: the skill suite is greenfield-only by explicit design (context-engineering = "for a *new* project"; "do not use for single-file edits to an existing CLAUDE.md"), but Rex tweaks the system continuously, so every project scaffolded against an older version silently drifts behind — and there's no signal for which projects are worst or what they're missing. This is an *ongoing maintenance* need, not a one-time cleanup. **Ruled out:** (B) full re-scaffold mode — fights the write-guard / markdown-only invariants (blind regen = the qventus-clobber class) AND the moving-target problem (standard changes every tweak); (C) a generic "point a workflow at anything" audit capability — premature generalization, violates Rule-of-Two. **Direction (confirmed by both pilots):** a read-only **audit** that diffs a project's actual context against a current-standard checklist, then **classifies each gap by KIND** (improvement / intentional-divergence / cosmetic / N/A). The classification — not the diff — is the load-bearing half. **Reports: [`docs/audits/2026-06-13-epost-context-drift.md`](docs/audits/2026-06-13-epost-context-drift.md) (#1, `epost-intelligence-feed` — Next.js, git, Codex-multi-agent), [`docs/audits/2026-06-13-field-society-demo-context-drift.md`](docs/audits/2026-06-13-field-society-demo-context-drift.md) (#2, `field-society-demo` — not-git, scaffold-only, single-agent), and [`docs/audits/2026-06-13-tex-context-drift.md`](docs/audits/2026-06-13-tex-context-drift.md) (#3, `tex` — hand-rolled "light ritual harness", NEVER scaffolded by CE → out of scope).** **Cross-pilot finding (the design):** the **safe-backport tier is rubric-stable** — 6 items classified identically across the two in-scope projects (timestamped+session-of-day retro convention, autonomy run-to-done charter, /compact-vs-/clear, the two missing session-discipline sections [read-before-write, checkpoint], /end-session bookend, +memory note w/ fit caveat). The **judgment tier flips on *project facts*, not the checklist** — hooks (real gap w/ git+.env vs premature w/o), memory-note fit (multi-agent vs single), and the non-Claude-read-only rule (epost: *intentional opposite policy* — Codex writes on purpose; field-society: *N/A* — no Codex at all; **same checklist item, both non-actionable, a naive diff flags both as defects**). The flips reduce to a **5-field project profile — `{under-git, multi-agent, has-product-code, has-secrets/.env, has-deploy-target}`.** **Pilot #3 added the missing GATE:** `tex` was never scaffolded by CE and deliberately uses a minimal hand-rolled harness, so a checklist diff produces a **false-positive cascade** (~12 intentional omissions reported as defects) and would tempt bloating a workspace kept minimal on purpose — actively *harmful*, not just noisy. So a **field 0 gate — `opted-into-the-standard?` (scaffolded-by-CE / intends to track it)** — must run *before* the 5-field profile; if no, report "not a drift target" and stop (at most a non-gating advisory). Final shape if ever built: **(0) gate on opted-in → (1) read 5-field profile → (2) diff → (3) classify by profile.** Step 0 is non-optional — it's the difference between a useful tool and a harmful one. *(Supporting evidence the need recurs: field-society already had a prior hand-audit, `docs/context-audit-2026-05-10.md`.)* **Rex's decisions 2026-06-13 ([D-013](docs/DECISIONS.md)):** (a) **fix the audited old projects by hand** from each report's "Open decisions" (Rex-side, his pace) — once brought current, they're current; (b) **skill DECLINED, not merely held** — automating a *one-time* catch-up across a handful of old projects never pays back its build cost, and the pilots found drift is mostly low-stakes. The full design (gated 4-step) survives in the reports + D-013 if ever wanted. **Revisit trigger (narrowed):** a change must sweep across *all* current projects at once AND by-hand propagation has become impractical — and even then the likely tool is *targeted propagation of a known delta*, a different shape than this diffuse drift auditor, so don't resurrect this design reflexively. By-hand + the documented method is the standing resort.
- **Validation finding: skill chain doesn't auto-compose; mental model mismatch + misleading repo name.** **Fix-candidate D SHIPPED 2026-06-14 ([D-014](docs/DECISIONS.md)):** context-engineering→DSB terminal-state handoff (output-summary §3 conditional successor step) + DSB "unrequested collision" decline-gate; prd-creator needed no edit (its output-summary already hardcodes the context-engineering handoff and forbids inventing other next steps). **Fix-candidate C (rename) HELD** as a separate Rex-gated decision — authoring verification refuted the backlog cost estimate (112 refs across 36 files, not ~15; a local-dir rename breaks the 3 skill symlinks *and* the context-engineering-audit skill's 6 hardcoded absolute paths; touches gated agent-config files). Authored via `/furnace-plan`, Cowork-reviewed across two rounds (caught the D-013/D-014 number collision + a misread "DSB is never named" claim — bucket-1 and bucket-2 per the furnace grading). Retro: [`docs/retros/2026-06-14-chain-handoffs-D.md`](docs/retros/2026-06-14-chain-handoffs-D.md). First observation: 2026-05-11 Squirreled validation. User invoked `/context-engineering` with a working brief, expecting the chain (prd-creator → context-engineering → design-system-bootstrap) to run as one orchestrated flow. The current architecture has three separate skills that hand off manually — composition is the user's responsibility. Result: user skipped prd-creator entirely. context-engineering gracefully accepted the brief in place of a PRD, but the resulting PRD will be thinner than a prd-creator interview would produce. *(2026-06-08 caveat: the Squirreled repo on disk shows the opposite — prd-creator ran, context-engineering didn't — so this "skipped" account is from a different/earlier attempt and was not reconciled. The finding stands regardless: it rests on the unenforced-handoff problem, which the disk confirms.)* **Two connected issues:** (1) sophisticated user mental model assumes one command runs the chain; (2) the repo name `prd-to-product` implies you start with a PRD when the actual starting point is a rough idea / brief — a more accurate name is `idea-to-product`. The rename reinforces the architectural fix: `/idea-to-product` is the natural name for a meta-skill that orchestrates. **Three fix candidates:** (A) build `/idea-to-product` meta-skill that orchestrates the chain; (B) smart routing in context-engineering cluster 0 — detect brief-vs-PRD and offer to call prd-creator first; (C) cheapest — rename the repo to `idea-to-product` + better cluster-0 question + README. Recommend C as first move, escalate to B if C doesn't solve, escalate to A if multi-step orchestration earns its keep. Rename cost: ~30 minutes (GitHub auto-redirects, ~15 in-repo find/replace, no symlink breakage). **(D) added 2026-06-11 from obra/superpowers review — terminal-state handoffs:** superpowers never built an orchestrator; each skill *ends by naming its successor* ("the ONLY skill you invoke after brainstorming is writing-plans") and a SessionStart hook injects the dispatcher. Cheaper than A, stronger than C: add an explicit "next skill" terminal state to prd-creator → context-engineering → design-system-bootstrap, each with a skip condition (qventus lesson: skip bootstrap when a design system already exists). D can ship with C; A stays the escalation if D's soft handoffs still get dropped. **Ready (moratorium lifted 2026-06-08):** Squirreled stalled at the PRD → context-engineering handoff, so the experiment that justified deferral never happened — and that stall *is* the strongest evidence for fix-candidate A. Worth its own iteration session. **qventus post-mortem (2026-06-08) tempers candidate A, does not kill it:** qventus ran prd-creator → context-engineering as smooth manual hand-offs, then *correctly declined* design-system-bootstrap because a complete design system already existed — a naive auto-running orchestrator would have clobbered the real tokens. If A is built it MUST branch "skip bootstrap if a design system already exists." See [`docs/retros/2026-06-08-qventus-post-mortem.md`](docs/retros/2026-06-08-qventus-post-mortem.md) §3. **Fix-candidate D enrichments (2026-06-12 harness batch):** (i) *cold-start sufficiency* — each skill's terminal state must be sufficient for a fresh agent to resume from files alone, not just name its successor (Ralph's loop-resumability test + arXiv 2603.25723's stage contracts); fold in when D is built. (ii) *Exclusion-keyword precedence* — promote each SKILL.md's "Do not use for" lines from advisory prose to hard precedence in routing (claude-code-harness's routing-rules manifest, where "exclusion keywords take absolute priority," plus a Reddit multi-agent practitioner's `00-scope.md` pattern); trigger: first observed mis-route among the three skills. Source: [`docs/retros/2026-06-12-harness-batch-review.md`](docs/retros/2026-06-12-harness-batch-review.md).
- **Validation finding: skills should auto-detect input, not require explicit attachment. SHIPPED 2026-06-14 ([D-015](docs/DECISIONS.md)).** prd-creator and context-engineering cluster 0 now scan the working directory (`docs/` + root) for source material and *offer* it for confirmation before asking (detect→offer→confirm, reusing DSB's existing scan pattern + its "default to asking, not detecting" principle; extends D-010, never silent-absorbs). context-engineering scans the exact `docs/PRD.md` path prd-creator emits, completing the chain auto-compose on the input side (pairs with D-014's handoff fix). **DSB deferred** this pass (already scans; its source is images/Figma, not repo markdown). Authored via `/furnace-plan`, Cowork-reviewed across two rounds (caught `notes.md` noise + an interpretive D-010 reading Rex resolved). Retro: [`docs/retros/2026-06-14-chain-handoffs-D.md`](docs/retros/2026-06-14-chain-handoffs-D.md). First observation: 2026-05-11 Squirreled validation. Current behavior: user must explicitly attach the brief with `@/path/to/brief.md` syntax when invoking `/prd-creator` or `/context-engineering`. User expectation: open the project folder, run `/prd-creator`, and have the skill find the brief in the working directory automatically. If found → confirm with user ("I see `docs/brief.md` — is this what you want to work from?"). If multiple candidates → list and ask which to use. If none → ask user whether to paste content, type from scratch, or proceed without source material. **Where to look:** project root, `docs/`, common filenames (`BRIEF.md`, `brief.md`, `*-brief.md`, `IDEA.md`). Connects to the chain-orchestration finding above (same root cause: skills require explicit input plumbing the user shouldn't have to think about). Fix surface: cluster 0 in both `prd-creator` and `context-engineering`. Likely also applies to `design-system-bootstrap` for brand assets. Ready to fix (moratorium lifted 2026-06-08: the Squirreled experiment stalled at the PRD handoff and never ran context-engineering, so "don't change the skill mid-experiment" no longer applies).
- **Verify `design-system-bootstrap` actually triggers and produces usable output.** Skill is built and symlinked but no retro covers its validation in this repo's discipline. Promote to In progress with a real test project. **qventus could NOT validate it (post-mortem 2026-06-08):** the one real project where it could have run *declined* it — qventus already had a complete hand-authored `--primary` token system from a Claude Design handoff, and the skill explicitly does not migrate an existing system. The reference layer that *was* hand-integrated there was later killed (D-033) once the static rewrite consumed none of it — evidence against a *separate* design-system layer for single-artifact prototypes, but the skill's from-scratch path remains untested. **Needs a bootstrap-from-nothing project.** See [`docs/retros/2026-06-08-qventus-post-mortem.md`](docs/retros/2026-06-08-qventus-post-mortem.md) §2.
- **Build-defaults item 5 (check/test pre-commit, prose-only).** Pilot pending item 1's real-project evidence. Brief: [`docs/build-defaults-brief.md`](docs/build-defaults-brief.md).
- **Build-defaults item 6 (defer abstraction).** Pilot pending items 1, 5.
- **Build-defaults item 2 (vertical slice).** Pilot pending items 1, 5, 6.
- **Build-defaults item 3 (test-first for logic).** Pilot pending earlier items.
- **`stack_summary_one_line` row missing for `stack=other + deploy_target=none`.** Decisions table in [`skills/context-engineering/generator/decisions.md`](skills/context-engineering/generator/decisions.md) covers Next/React/Node/Python permutations but not `other + none`. One observed instance (Phase 2 dog-food). Fix on second instance per continuous-mode discipline.
- **Skill ecosystem gaps (Thariq 9-category framework).** Eight uncovered categories: Library/API Reference, Product Verification, Data Fetching, Business Process Automation, Code Quality/Review, CI/CD, Runbooks, Infrastructure Ops. Promote to In progress only when a real failure surfaces. Existing three skills all fall in Code Scaffolding.
- **Sibling skill: `red-team` (adversarial review) — RUNG 1 SHIPPED 2026-06-15 ([D-016](docs/DECISIONS.md)); sibling skill itself still PARKED.** **Escalation ladder (chosen over building the skill outright):** *Rung 1 (shipped)* — prd-creator's terminal state (`output-summary.md`) now conditionally recommends a tool-agnostic critique pass before the context-engineering handoff, gated on an **identity-defining-and-in-tension-with-a-V1-cut** bet; closes the observed "critique before locking in" hole cheaply, reusing the D-014/D-015 successor-handoff pattern, without building a skill whose niche the now-rich ecosystem (devil's-advocate, plan-review, llm-council) may already cover. *Rung 2 (parked)* — build the `/red-team` sibling; **promote when** the soft handoff is observed dropped in a real run. *Rung 3 (rejected)* — fold the critique *work* into prd-creator (single-purpose violation). **Out of scope for Rung 1, deferred:** context-engineering's scaffolded ROADMAP critique — no observed-failure evidence yet (Rule of Two; promote when one surfaces). Authored via `/furnace-plan`, Cowork-reviewed across two rounds (round 1: one bucket-2 contradiction + an over-broad trigger; round 2: bucket-3 refinements only). Retro: [`docs/retros/2026-06-15-red-team-rung-1.md`](docs/retros/2026-06-15-red-team-rung-1.md). **Original framing (kept for the parked sibling):** a new skill that takes any document — PRD, scaffolded ROADMAP, design rationale, brief, brand statement — and returns critique. Sibling to prd-creator / context-engineering / design-system-bootstrap rather than a feature inside any of them, because the capability is generic across documents and each existing skill should stay single-purpose. **First real-failure evidence landed 2026-05-11 Squirreled validation:** prd-creator collected answers but didn't pressure-test the V1 plan. User reached for `/brainstorm-ideas-existing` mid-interview to fill the gap. The brainstorm caught a load-bearing differentiator slip ("forced interpretation" → "synthesis quality") that prd-creator missed entirely, plus structural V1 issues (mobile capture, vector embeddings, synthesis output shape, cold-start story). This is no longer speculation — the chain has a hole at "critique before locking in." Likely fix: build `/red-team` OR fold a critique cluster into prd-creator's late flow before cluster 5 (decisions consolidation). Lean toward the sibling skill for the same single-purpose reasons originally cited. Source: 2026-05-11 ideation conversation (#15 Creative Red Team + #18 Narrative Gap Finder) confirmed by 2026-05-11 Squirreled validation. **Output-contract enrichment (2026-06-12 harness batch):** when built, adopt a three-state verdict schema (APPROVE / REQUEST_CHANGES / decision_needed) with evidence-required approval — "APPROVE output with empty evidence is prohibited" — and minor findings preserved in non-gating arrays rather than filtered out; per claude-code-harness's `review-result.v1`, a third independent source for the claim→evidence gate (alongside superpowers Pattern 3 and the 2026-06-08 council).
- **Agent-process & context-harness upgrades — ready, no blocker.** Source: Séance (iOS) session hardening the agent-process layer; full reasoning in [`docs/agent-process-brief.md`](docs/agent-process-brief.md). Five proposed additions to `context-engineering`, in value order: (1) scaffold `/end-session` bookend command + upgrade retro convention to timestamped H1 + session-of-day (fixes same-day "not done this session" misread) — **DONE 2026-06-08**: retro convention (timestamped-H1 + session-of-day + before-commit-bundle + non-trivial-only) formalized in the retro README template/example/this repo, and the `/end-session` bookend command built (template + example + this repo's copy, registered in the generator manifest, cross-referenced from session-discipline + principles). Pairs with the session-start git-history selection fix shipped same day; (2) seed `permissions.allow` in `claude-settings.json.template` from the already-captured `{{install/dev/check/test_cmd}}` + a universal-safe read-only core, defer the rest to `fewer-permission-prompts` — **DONE 2026-06-08** (template + example seeded; drop-if-none + emit-without-hooks rules in `generator/decisions.md`; *stale-note correction 2026-06-12: this repo's `.claude/settings.json` allowlist is in fact seeded — the "still pending" flag was stale*); (3) consolidate the scattered autonomy rules into one "run to done" charter with explicit precedence — *scope limits are the outer gate, run-to-done governs inside them* — plus a cluster-5 interview question for the gated-surface default split — **DONE 2026-06-08** (Autonomy section added to `session-discipline.md.template` + flat-CLAUDE + example + this repo; Q27b + `autonomy_gate_override` added to intake/decisions; visual-gate clause is OPTIONAL on `uses_visual_confirmation_gate`, verified one gated bullet renders per gate value); (4) **reconcile the memory model** — rewrite `principles.md:19–25` and add an AGENTS.md "where facts live" note to one rule: *repo (AGENTS.md/docs) = anything that survives a tool switch (Codex/Cursor can't read Claude's machine-local auto-memory); auto-memory = Claude-local scratch only* — **DONE 2026-06-08** (principles reframed to survives-a-tool-switch; "Where facts live" section added to AGENTS.md.template + flat-CLAUDE + example + this repo); (5) harness-condition the `session-start` AGENTS.md read on `rule_shape` (skip in flat shape where rules auto-load via CLAUDE.md) — **DONE 2026-06-08**: `<!-- OPTIONAL: agents_read_step -->` added to [`session-start.md.template`](skills/context-engineering/templates/claude-commands/session-start.md.template) gated on `rule_shape == "modular"`; flat-shape `examples/output-small` regenerated (AGENTS.md step dropped, list renumbered 1–3); rule documented in [`generator/decisions.md`](skills/context-engineering/generator/decisions.md) "session-start AGENTS.md read is rule-shape-conditional"; dry-run flat substitution diffs clean against the example, modular unchanged. *Stale-note correction 2026-06-12: this repo's `.claude/commands/session-start.md` is in fact already the 3-step version with no AGENTS.md read — the "still pending" flag was stale.* **Groups 1–5 shipped 2026-06-08** (group 5's in-repo dog-food self-edit is the only loose end). Connects to the on-demand-hooks open decision below and the BACKLOG-scaffolding in-progress item.
- **Skill injection by project type — domain-layer companion to the agent-process item above. Not started; design question must be resolved first.** Source: [`/Users/rexc/Sites/seance/docs/references/skill-injection-by-project-type.md`](file:///Users/rexc/Sites/seance/docs/references/skill-injection-by-project-type.md) (reviewed 2026-06-08). Where the item above is the *process* layer (always-on, stack-independent — `/session-start`, `/end-session`, hooks, permissions), this is the *domain* layer: teach `context-engineering` to map a new project's **stack + primary output** → a curated skill shortlist, install a tiny **active** set into `.claude/skills/`, **park** the rest in `docs/references/staged-skills/`, and **promote per phase** via ROADMAP notes. Generalizes what Séance did by hand (its `staged-skills/README.md` + per-phase ROADMAP promotion notes are the working reference implementation). Capability is greenfield on top of the one existing hook — Q27a `external_skill_references` ([`intake.md:215`](skills/context-engineering/generator/intake.md)) only cross-links *already-installed* global skills; there is no stack→skill map, no manifest, no active/parked/promote concept. **Blocking design question (resolve before building):** the doc's promote-mechanic assumes you *vendor* repo files and `cp -R` them into `.claude/skills/`, but most candidate skills now arrive as **marketplace plugins** (`anthropic-skills:*`, `design:*`, `data:*`, etc. — ~70 plugin descriptions load every session, the exact context-cost the doc warns about, observed live 2026-06-08). For plugin-sourced skills "active vs parked" is enable/disable, not a filesystem move, and may not be per-project controllable at all. The manifest schema anticipates this with a `source` field (`plugin | marketplace | repo-vendored | url`) but the §4 flow only solves the vendored case. **Don't build until the source-dependent promotion mechanic is designed** — otherwise it ships solving one source. **Scope when it does land:** large (>300-line feature gate) + new generated-artifact type (architecture decision) — needs explicit scope confirmation and likely phasing. For *this* repo specifically the domain layer is a near-no-op (skill-building project → active `skill-creator`, parked `mcp-builder`, both already present as plugins). Defer to `find-skills` for the long tail (doc §6).
- **Broader Appendix-A `/session-start` rewrite — easy, low value, do only if session-start bloat bites.** Group 5 (shipped 2026-06-08) fixed one redundant read (AGENTS.md in flat shape). `agent-process-brief.md` §1.1 / Appendix A proposed the fuller redesign we only partially did: restructure the command from "read 3–4 files every time" into tiers — don't re-read *anything* that auto-loads; **always** read just the latest retro (carries the handoff); read ROADMAP + `DECISIONS_ACTIVE` **only when resuming feature work**, skip for a trivial one-off. Pure markdown template rewrite, target wording already exists in Appendix A; ~3 files + a gated self-edit to this repo's own copy; ≈half a session. **Why deferred, not done:** group 5 already captured the high-value slice; the rest is marginal token savings traded against asking the agent to judge "am I resuming feature work," which can misfire. Promote only if `/session-start` weight becomes a real problem. **Baseline-check candidate added 2026-06-12 (harness batch):** the scaffolded `/session-start` should run the project's `check`/`test_cmd` before any work and reject a broken starting state — compounding bugs across sessions is the named failure (both Anthropic long-running-agent posts; celesteanders/harness implements it as a session-protocol step). Gated on the project actually having such a command (this repo has none); fold into the next incidental edit of the template or this item's rewrite, whichever comes first.
- **No modular-shape example output tree for regression — medium effort + ongoing tax; build only if modular work picks up.** The verification contract (CLAUDE.md "Verification before claiming done") diffs dry-run output against [`examples/output-small/`](skills/context-engineering/examples/output-small/) — but that's the *only* example and it's **flat** shape. There is no **modular** example (the shape where rules live in `.claude/rules/*.md`, AGENTS.md is canonical, CLAUDE.md is `@AGENTS.md`). Consequence, seen live in group 5: changes to the modular path can't be diff-verified, only inspected. Closing it = build a committed `examples/output-modular/` from a representative modular input (e.g. tokens-plus-linter design, or ≥1 AI surface), hand-rendering the full tree correctly (AGENTS.md + one-line CLAUDE.md + several `.claude/rules/*.md` + docs + commands + settings + hooks). **Cost:** the baseline must be trustworthy (a wrong baseline is worse than none), and every future template change then has to update *two* trees. **Why deferred:** flat is the common case, modular is rarer, and one good baseline beats two half-maintained ones. Promote when modular-affecting changes become routine. **Council caveat (2026-06-08, [`council-report-2026-06-08.html`](docs/council/council-report-2026-06-08.html)):** do NOT build this as a golden-tree *byte-diff*. Generation is LLM-driven and non-deterministic — a byte-diff fires false positives on legitimate run-to-run variation and trains the operator to ignore it (the opposite of the trust it's meant to protect). Build it as **structural/invariant assertions** instead: required files exist, required sections present, cross-references resolve, OPTIONAL gates evaluated correctly. Assert on structure, not exact bytes.
- **Invariant/semantic output checks — council's "a day" layer. (Write-guard prose AND enforcement hook are DONE.)** Source: LLM Council on improving the kit ([`council-report-2026-06-08.html`](docs/council/council-report-2026-06-08.html) / [`council-transcript-2026-06-08.md`](docs/council/council-transcript-2026-06-08.md)); decisions **D-005** (prose) + **D-006** (hook). **DONE 2026-06-08:** the non-destructive write guard ships as prose across all three skills AND is **hook-enforced** by [`hooks/write-guard.sh`](hooks/write-guard.sh) (global PreToolUse `Write|Edit|MultiEdit`, interactive-`ask` / headless-`deny`, hook-maintained run-owned set; 15/15 unit tests + true end-to-end verified — see **D-006** and [`docs/retros/2026-06-08-write-guard-hook.md`](docs/retros/2026-06-08-write-guard-hook.md)). The qventus clobber class is now **enforced (interactive) / refused (headless)**, not merely mitigated. **STILL OPEN — (b) invariant/semantic checks:** no-jargon-leak (grep "Cluster 0"/"D-NNN" against emitted copy — the prd-creator vocabulary-leak finding), provenance-grounded (every cited fact traces to a given input — the temporal-hallucination finding). *Enrichment (2026-06-12 harness batch): unknowns/stop-conditions as a scaffolded template section — claude-code-harness's `spec.md` carries unknowns and stop conditions structurally, so an invented fact becomes a schema violation rather than a prose sin; feeds the provenance-grounded check.* **`/idea-to-product` orchestrator-gate update:** the gate is **CLEARED for the clobber-safety class only** — headless runs are *update-incapable* on pre-existing files (they `deny`), so an unattended orchestrator is non-destructive *because* it cannot modify existing files, NOT because its full job runs unattended (D-006 headless ceiling). Two residuals before an orchestrator: the hook is installed only on the operator's machine (not distribution-portable — a shipped plugin would install it at plugin-install time), and a generator that forgets to arm has no enforcement (prose backstop).
- **Token-adopt command for Claude Design bundles — deferred to bundle #2 (Rule of Two).** Per [D-008](docs/DECISIONS.md), the kit builds no adopt-skill / no DSB adopt-mode / no command now: Claude Design bundles stay authoritative, tokens reach a real product via a one-time product-side `cp`. The fidelity test ([`docs/design-handoff-fidelity-test.md`](docs/design-handoff-fidelity-test.md)) showed values are lossless but structure is non-isomorphic to the scale-first template — at N=1 you can't separate a shipped theme from tweak-tool scratch, so any encoded schema is unvalidated. Baseline + manual playbook + bundle ledger recorded in [`docs/design-handoff-adoption.md`](docs/design-handoff-adoption.md). **Promotion trigger:** a 2nd real Claude Design bundle arrives — fill the ledger's second row, re-derive against row 1, and decide whether a token-adopt command earns its keep (defaults to CSS-vars, never Tailwind). Source: [audit council](docs/council/council-report-2026-06-09-audit.html) + [retro](docs/retros/2026-06-09-claude-design-adopt-council.md).
- **Pressure-test behavior-shaping prose before shipping it — verification-gap candidate from obra/superpowers review (2026-06-11).** Our verification contract covers template *substitution* (dry-run diff vs `examples/output-small/`) and hooks (live-fire), but nothing tests whether behavior-shaping prose (principles, session-discipline rules, scaffolded `.claude/rules/`) actually changes agent behavior — we ship those on inspection. Superpowers' `writing-skills` treats skill prose as code: baseline-test a subagent *without* the rule, document its failure/rationalizations verbatim, write the rule to counter them, re-test ([skills/writing-skills/SKILL.md + testing-skills-with-subagents.md](https://github.com/obra/superpowers/tree/main/skills/writing-skills)). Complements the 2026-06-08 council's "structural assertions, not byte-diffs" finding on the behavioral side. **Adopt in slices, not as ceremony:** promotion trigger = the next time a discipline rule is written or materially edited, run one baseline scenario first; if two such tests catch a real prose defect, formalize the step into the verification contract (Rule of Two). **Trigger fired 2026-06-11 (A/F/G discipline rules written), test deliberately skipped — counter stays 0/2:** an n=1-per-condition run can't attribute a behavior delta to the rule (the same run-to-run-variation confound the 2026-06-08 council ruled out for byte-diffs), and the design's load-bearing assumption — that Task-tool subagents auto-load CLAUDE.md in this harness — is unverified. Preconditions for any future slice: verify the auto-load assumption first; run ≥3 trials per condition; only a consistent delta touches the counter. A noisy 1/2 would be manufactured evidence — the exact ceremony-accretion this item's own caution warns against. **Method detail for future slices (reconciled 2026-06-11 from the [superpowers handoff brief](docs/superpowers-context-engineering-handoff.md), a second independent extraction of the same source — the convergence signal the 6-11 retro said to watch for):** pressure scenarios combine 3+ pressures (time, sunk cost, authority, social), force a concrete A/B/C choice with no "I'd ask the user" exit, and frame as real; meta-testing asks the violating agent "how could the rule have been written to make the right answer unambiguous" (three diagnostic answers: ignored-it → foundational principle; should-have-said-X → add X verbatim; didn't-see-Y → surface it earlier); bulletproof = complies under max pressure AND cites the rule AND acknowledges the temptation. **Caution logged:** adopt their *method*, not their persuasion-heavy tone — our retro failure-tags don't yet show the failure classes their Iron-Law armor defends against; importing it without baseline evidence is the ceremony-accretion the failure-tag instrument exists to catch. **Method footnote (2026-06-12, arXiv 2603.25723):** when a slice does run, record per-scenario *flips* rather than aggregate deltas, and aim scenarios at boundary cases — the paper found harness effects concentrate on boundary instances while most instances don't flip, so aggregate deltas at n=3 read as noise; reinforces (does not change) the ≥3-trials precondition.
- **Superpowers liftables — fold into the next *incidental* edit of the relevant file (no dedicated session, per harness-proposals council guidance):** (a) *rationalization tables* — extend "every rule cites its failure mode" with the excuse the agent actually used, harvested from recurring retro failure-tags once ~10 sessions accumulate *(the [handoff brief](docs/superpowers-context-engineering-handoff.md) Pattern 2 carries the full four-way plug — explicit negation, Excuse→Reality row, red-flags list, trigger updated with violation symptoms — and aims it at a second surface, the rules context-engineering* emits*; that scaffold-level variant lives in the dedicated entry below)*; (b) *"plan failures" enumeration* for context-engineering's ROADMAP template conventions (name the placeholder sins: "TBD", "add appropriate error handling", "similar to Task N") + bite-sized step granularity (one 2–5-min action, exact paths, expected output) — *2026-06-12 addition: "display-only stub" (UI renders, does nothing) joins the sins list, per effective-harnesses' feature-stubbing failure + Ralph's anti-placeholder prose, second/third independent sources*; (c) *word-count budgets* for always-loaded content (superpowers targets: <150 words always-loaded, <200 frequently-loaded, <500 otherwise, verified with `wc -w`) — gives the deferred CLAUDE.md-slimming item (E) and the Appendix-A session-start rewrite a measurable bar — *2026-06-12 addition: a reverse-engineered ~250-char harness-enforced cap on skill descriptions (sidbharath's Anatomy of Claude Code) joins the budgets; verify before relying on it (reverse-engineering, not docs), but note design-system-bootstrap's post-D-011 description sits at 478 chars and would be silently truncated — mangling trigger phrases — if the cap is real*; (d) *instruction-priority preamble* (user instructions > skills > defaults) for scaffolded session-discipline. Source: [github.com/obra/superpowers](https://github.com/obra/superpowers), reviewed 2026-06-11.
- **animations.dev Part 1 skill-craft patterns — fold into the next *incidental* skill-template edit (no dedicated session, per harness-proposals council guidance).** Anti-example output contracts, canned initial response, narrow verb-specific triggers — preserved with provenance in [`docs/animation-taste-reference.md`](docs/animation-taste-reference.md) Part 1. *(Part 2's motion content shipped 2026-06-11 as the DSB motion layer — easing bank + semantic easing tokens, duration bands, frequency rule, all three Q7c reduced-motion strategies wired; see [`docs/retros/2026-06-11-dsb-motion-layer.md`](docs/retros/2026-06-11-dsb-motion-layer.md). The shipped layer remains live evidence for the pending DSB HTML-supplement decision above — easing/duration can only be *felt* in a live preview, not read in markdown.)*
- **Scaffold-level candidates from the superpowers handoff brief — three generated-artifact upgrades for `context-engineering`, parked pending its next substantive template session.** Source: [`docs/superpowers-context-engineering-handoff.md`](docs/superpowers-context-engineering-handoff.md) (reconciled 2026-06-11 — a second independent extraction of obra/superpowers that converged on items above; convergence is promotion evidence per the 6-11 retro). The three: **(1)** scaffolded discipline rules ship pre-hardened — Excuse→Reality table + red-flags list seeded from known rationalizations (Pattern 2's four-way plug, applied to *emitted* rules — distinct from the liftables item's this-repo surface); **(2)** a project-agnostic verification-gate rule in the default `.claude/rules/` scaffold carrying the claim→evidence mapping (tests pass → output with 0 failures, not "should pass"; subagent done → diff inspected, not its report), plus gating the generator's own "scaffold complete" claim (Pattern 3); **(3)** a ready-made adversarial review-subagent prompt template in scaffolded `docs/` — inputs, severity calibration, strengths-first, hard verdict contract (Pattern 4; rhymes with the red-team sibling item but is a cheaper shipped-artifact slice). Brief's own constraints bind: harvest don't install, drop the superpowers tone, adapt to existing template structure after reading the whole skill, and pressure-test at least one generated artifact before calling it done (which re-fires item 32's trigger, preconditions and all). **Promotion trigger:** the next session that substantively edits context-engineering's templates, or a scaffolded project exhibiting the failure one of these prevents. **2026-06-12 enrichments riding the same trigger ([harness-batch retro](docs/retros/2026-06-12-harness-batch-review.md)):** pre-seed candidate 3's review-subagent template with documented evaluator failure modes (self-justification of found issues, superficial coverage) and an expect-calibration-rounds-before-trusting-verdicts note (Anthropic effective-harnesses); candidate 2's claim→evidence gate gains per-task acceptance criteria phrased so a fresh read-only subagent can check them one-by-one against a structured verdict (celesteanders/harness — its one real composition beyond the primaries); and audit the emitted commands/rules (`/session-start`, `/end-session`, scaffolded `.claude/rules/`) for explicitness against arXiv 2603.25723's six-component checklist — contracts, roles, stage structure, adapters, state semantics, failure taxonomies.
- **Harness-batch liftables (2026-06-12) — trigger-gated candidates from the external-source batch that fit no existing entry; fold into incidental edits, no dedicated session.** Source: 2026-06-12 review of 9 sources (7 URLs + 2 Reddit posts), [`docs/retros/2026-06-12-harness-batch-review.md`](docs/retros/2026-06-12-harness-batch-review.md). Four candidates: **(a)** *stale-assumption tagging* (Anthropic effective-harnesses) — rules may additionally name the model-capability assumption they encode, making them retire-able when models improve (their sprint decomposition went obsolete on a model upgrade); a one-line extension to "every rule cites its failure mode"; trigger: next major model change in the harness, or first never-firing rule observed in retros. **(b)** *`docs/references/*-llms.txt` slot* (OpenAI harness-engineering) — scaffold a place to vendor load-bearing dependency docs into a project, plus an intake question for which dependencies qualify; trigger: first observed dependency-API hallucination in a scaffolded project. **(c)** *doc-gardening* (OpenAI) — a scaffolded `/doc-audit` command sweeping for stale docs and dead cross-references; trigger: retro failure-tags show lost-context / stale-cross-reference recurring (the kill-watch instrument gates this class — counter, not instinct). **(d)** *feature ledger + never-weaken-tests rule* (Anthropic harness-design-long-running-apps) — a markdown (per the invariant, not JSON) per-item ledger with verification steps + explicit pass state, for scaffolded projects expecting unattended runs; and a "never remove or weaken tests" rule (named failure: agent silently deletes failing checks to declare victory — a goal-drift instance under the B taxonomy) for scaffolded projects with test suites; triggers: a premature-completion retro tag / first observed test-editing instance. Honesty note from celesteanders/harness: its advertised "append-only" task-list enforcement was prompt-only and its claimed lint hook is absent from the artifact — if (d) ever ships, enforcement claims must match mechanism.
- **DSB HTML supplement — deferred indefinitely, deliberately at the bottom of the pile ([D-012](docs/DECISIONS.md), 2026-06-11).** The investigation ([memo](docs/html-over-markdown-recommendation.md)) recommended it; a devils-advocate pass returned Reconsider (the "anyone opens DSB output" assumption is unobserved — the skill has never run for real); Rex's gut call: not necessary, may never look at it. Markdown-only invariant stands, no carve-out, prd-creator export stays dead. **Sole promotion trigger:** DSB's first real bootstrap-from-nothing run includes a *hand-built one-off* HTML preview (no generator changes) — only observed use of that one-off reopens the template question. Do not resurrect from the memo's recommendation alone.
- **Candidate future products parked for exploration.** Three product briefs captured in [`docs/product-briefs/`](docs/product-briefs/) for future PRD development: AI Research Synthesizer, Anti-Generic Machine, Design Process Black Box Recorder. The Black Box Recorder is the most ambitious — multi-system passive-capture product, parked deliberately as a long-horizon idea. None are scoped as next-build; revisit after Taste Builder validation completes.
- **Agent-teams / subagent guidance in scaffolded projects — future scoped investigation (re-filed from "open decisions" 2026-06-09).** Upstream moved: the harness now ships first-class `Agent`/`Workflow` orchestration, so the old watch condition ("when agent teams moves from experimental to standard") has partly triggered. Open question: should `context-engineering` scaffold subagent/workflow *guidance* into a project's rules, and if so for which project types? Not folded in yet — its own investigation, promote when a scaffolded project actually needs multi-agent structure. Scope likely medium+ (new generated guidance, possibly an intake question). Connects to the "Skill injection by project type" item above (same domain-layer surface).
- **Harness-proposals review (2026-06-09) — adopted-but-deferred set + kill-watch.** A Claude review of 5 first-party Claude Code blog posts produced 8 proposals (A–H); an LLM council pressure-tested them. **Killed:** C (verifier-subagent-per-rule + skeptic pass) and H (keep CLAUDE.md static for prompt-cache stability) — both scaled-team defense-in-depth imported onto a one-human zero-traffic repo; H's cache premise also describes Anthropic's infra, not this local harness (unverified, and the saving is imperceptible for a file one person loads a few times a day). **Already parked:** D (`/freeze` / `/careful` scoped hooks) = the "on-demand hook scaffolds" open decision below; council independently reaffirmed the defer. **DONE this session:** the retro failure-tag instrument (commit `4fb6a57`). **Landed 2026-06-11 (Rex-directed bundle — his direction was the human gate the no-dedicated-session guidance assumed; the shared "next incidental edit" trigger had deadlocked):** (A) non–Claude-Code agents read-only rule in CLAUDE.md next to the self-modification gate; (B) the *agentic-laziness / self-preferential-bias / goal-drift* taxonomy in `skills/context-engineering/principles.md` as a lens above — never replacing — each rule's cited failure; (F-cheap) the conclusion-not-artifact subagent heuristic in session management (shipped as prose, untested — see the pressure-testing item above); (G) the `/compact`-vs-`/clear` cut. The no-sunset-clause finding was honored: first move was a cut (the duplicated "Direct on main" line left the top anchor; the council-block trim was examined and declined — its calibration examples aren't reconstructable from D-009). See [`docs/retros/2026-06-11-deferred-bundle-and-cso-audit.md`](docs/retros/2026-06-11-deferred-bundle-and-cso-audit.md). **(E) SHIPPED 2026-06-12 (2nd session, Rex-gated):** CLAUDE.md slimmed 158→135 lines / 1,836→1,181 words (−36%) — every binding rule kept inline with its failure-mode citation; rationale, history, and attribution cut to plain pointers (D-009, `docs/retros/README.md`, `docs/agent-process-brief.md`); **no `@include`** (imports load content into context, defeating the slim — the Anatomy mechanism was rejected, not just unverified). Devils-advocate pass per the D-009 reversible-prose calibration returned REVISE with an 8-item punch list, all applied; headline catch: the draft had cut the four council fork examples while pointing at D-009, which verifiably does not contain them — restored inline (pattern: cuts removed *examples* while keeping *principles*; examples are the trigger-recognition mechanism). **Honest close-out per the DA:** the cited bars (~100-line TOC, 120-line cap, <150 words always-loaded) were **not met and are structurally unreachable** under the every-rule-cites-its-failure-mode invariant — dilution *mitigated, not solved*; and this was a harmless reversible tidy-up on external-convergence evidence, not an observed-failure fix (the failure-tag log has never recorded instruction dilution — which is also the one failure class the tag instrument can't self-observe). Retro: [`docs/retros/2026-06-12-claude-md-slim.md`](docs/retros/2026-06-12-claude-md-slim.md). *E promotion-evidence note (2026-06-12 harness batch): four independent sources converged on slim-context/progressive-disclosure — OpenAI harness-engineering (their monolithic AGENTS.md failed for four named reasons: crowds out the task, everything-important = nothing-important, rots instantly, unverifiable; their fix — a ~100-line table-of-contents over a structured `docs/` tree — is already this skill's scaffold shape, so the gap is this repo's own CLAUDE.md), a Reddit multi-agent practitioner post (120-line cap, context dilution observed firsthand), sidbharath's Anatomy of Claude Code (`@include` to 5 levels as a candidate mechanism — verify it actually resolves in this harness before designing around it), and Ralph (quality degrades ~147k tokens). A second Reddit post was excluded as derivative of the same primaries — independence rule: downstream syntheses don't count toward convergence. Per the convergence-as-promotion-evidence pattern (6-11 retro), E moves from "deliberate pass" to **promotion evidence met, awaiting Rex decision**; still possibly council-worthy.* **Kill-watch — the instrument earns or kills these:** after ~10 non-trivial sessions, read the retro "Failure this session" tags — if bad-substitution / unverified-rule failures recur, revisit C; if lost-context recurs, consider deepening E beyond the shipped 36% trim; if goal-drift / bad-substitution / lost-context never appear, C stays dead (E shipped 2026-06-12 as a tidy-up, not a guardrail). *(Kill-watch empirical support, 2026-06-12: arXiv 2603.25723 found full harnesses changed process metrics — tokens, calls, runtime — while resolved rates stayed roughly flat; "extra process layers can make a run more structured and locally convincing while still drifting from the acceptance object." Benchmark data behind the instrument's premise; C and H stay killed.)* **Meta-lesson logged:** this council was itself a D-009 over-application (reversible markdown prose) — for reversible-but-fuzzy calls reach for `devils-advocate` (cheap) before `llm-council`; reserve the council for costly+irreversible forks D-009 actually names. Source: 2026-06-09 harness-proposals review memo (in this session's outputs, not committed) + LLM council transcript [`docs/council/council-transcript-2026-06-09-harness-proposals.md`](docs/council/council-transcript-2026-06-09-harness-proposals.md).
- **Plan-review history mining — DONE 2026-06-13, superseded by the furnace-plan trial (In progress, top).** Ran the round-based extraction over 83 sessions, produced [`PATTERNS.md`](research/feedback-extracts/PATTERNS.md), councilled the build decision, and shipped the narrow furnace as a Cowork-graded trial. The self-healing-*loop* was NOT built — the council/audit resolved it into a pre-filter furnace instead. Candidate `D-NNN` ("mine history before building the loop") remains Rex-gated and unwritten; the resolution is recorded in the retro + PATTERNS.md rather than DECISIONS. Full record: [`docs/retros/2026-06-13-furnace-plan-trial.md`](docs/retros/2026-06-13-furnace-plan-trial.md).
- **Open decisions from prior roadmap.**
  - *Resolved 2026-06-11 — HTML output for `prd-creator` / `design-system-bootstrap`: **deferred indefinitely** per [D-012](docs/DECISIONS.md). Markdown-only stands; sole reopen path is the one-off-first promotion trigger in the backlog entry above.*
  - Should there be on-demand hook scaffolds in the skill? Pending a real failure mode (e.g., user repeatedly hits a moment where a scoped `/careful` would have helped). (Reaffirmed deferred 2026-06-09 — the write-guard hook covers the high-value class; Rule-of-Two not met.)
  - *Resolved 2026-06-09 — Verification skill: **skip.** The CLAUDE.md "Verification before claiming done" prose contract + dry-run-against-`examples/` already cover it, a `verify` skill exists in the ecosystem, and no continuous-mode failure pile-up was observed. See retro [`docs/retros/2026-06-09-html-over-markdown-recommendation.md`](docs/retros/2026-06-09-html-over-markdown-recommendation.md).*

## Done

See [`docs/retros/`](docs/retros/) for the session-by-session record. Skill-build Phases 1–4 (2026-05-10) and continuous-mode sessions (2026-05-10 → 2026-05-11) are recorded there.

## Format

Mid-session deferrals: log here immediately with one line on what + one line on what would promote it to In progress. Resolved items move to a retro, not back to this file.

In-progress entries name a promotion criterion ("when X happens, this is done"). Backlog entries name a promotion trigger ("when X observed, move to In progress").

## Cross-references

- Project rules: [`CLAUDE.md`](CLAUDE.md).
- Active decisions: [`docs/DECISIONS_ACTIVE.md`](docs/DECISIONS_ACTIVE.md).
- Decisions log: [`docs/DECISIONS.md`](docs/DECISIONS.md).
- Session retros: [`docs/retros/`](docs/retros/).
