# Retro — 2026-07-01 21:48 CDT — Remainder mines (CE-plugin, intent) + router: the tests-are-IP day   (2nd session of the day)

**Dominant failure tag: none** — with one accretion watch-item recorded below.

## What happened

Three `/mine` runs, three adoption commits, every adoption through propose-and-wait:

1. **CE-plugin remainder** — six blind readers over the 23 unmined skills, 15 solutions docs (board row's "19" corrected — 15 exist), behavioral test harnesses, and the cross-host converter. Yield: chain prior-art #2 (lfg), the +38% body-size measurement for fork 1, the drift-triage vocabulary, the delivery-decision evidence closed-form, /mine hardening gate tripped → `backlog`, 2 new watching rows.
2. **TanStack/intent remainder** — three readers over the ~140 files the 06-30 pass skipped, motivated by the CE run proving test suites are the densest IP layer. Confirmed twice over: the eval harness turned out to be a shipping instance of the pressure-test experiment (ticket now **design-complete**); Codex blocking hooks implementation-confirmed; the prior mine's 6-way failure taxonomy corrected to 13 classes.
3. **TanStack/router** (fresh clone, `ecbbd9a8`) — four readers; third falsified prior in a row ("app-code library, little to transfer" → the first production-scale deployment of the intent pipeline: 30 shipped skills, v4 check-skills workflow, plus Seq-1 CI/e2e/supply-chain fuel). Fork 1 got production data: the 500-cap and `references/` extraction compose.

**Board state:** `next` is Seq 1 project-setup design → Seq 2 skill-craft consolidation (inputs now complete and heavily armed). Rex also asked mid-session which other sources were bypassed; the coverage audit of all mined docs found exactly one real gap (intent's skipped files), which became mine #2 — the other repos' skips were all trigger-parked or justified out-of-scope.

## Patterns worth keeping

- **Tests are the densest IP layer.** Drove two of three mines and paid both times (seam-parity, shell denylist, deny-reason self-reference trap, prose-invariant locks, grader calibration — all from test files). Now a standing /mine heuristic: a source's test suite is never "mechanical coverage," it's where discipline gets enforced with incident citations.
- **Prior-falsification is 3-for-3 on Track 2** (template, workflow/devtools, router — and both remainders). No repo's name-level description has predicted its yield yet. The near-miss lesson from the 1st-session retro has graduated from "recorded pattern" to "empirically undefeated."
- **Remainder mines out-yielded their first passes** in both cases — the deliberate-stop coverage statements were honest but wrong about the tail's value.

## Watch-item (recorded, not yet a failure)

**Row-gloss accretion.** The "adds evidence to" adoption pattern is thickening the board: several rows now carry 3–4 mine refs and multi-clause glosses (project-setup, brownfield, docs-craft, chain-handoff). Each edit was individually justified; the sum trends toward the D-048 bloat class the board format exists to prevent. Two natural relief valves: the consolidation pass (Seq 2) retires several of these rows outright, and any row whose gloss outgrows one readable line should get thinned to a ticket on next touch (thin-on-touch already binds). If a 3rd session adds refs to the same rows without retiring any, tag it scope-creep then.

## Verification — what it did and didn't cover

- **Did:** 56 load-bearing citation spot-checks across the three mines (19 + 17 + 20) against pinned clones — all confirmed, including the refutations (ce-strategy's anti-scope optional; the "8KB Codex cap" folklore; both sides of the lfg `git add -A` nuance). Blind-reader independence held (facts + dedup inventory, never valuations). `render-backlog-kanban.py` after every board edit (0 tag warnings throughout); `check-live-links.py` clean (116 docs) after every doc batch.
- **Did NOT:** reader citations beyond the 56 were accepted on reader authority; router's `examples/` and docs trees were read structurally/representatively, not exhaustively (declared in the mined doc); CE-plugin's ~94 historical plan docs deliberately unread (their own repo's evidence says requirements docs are design history). No skill/template product files were edited — everything landed as mined docs, board rows, and one ticket fold — so no dry-run substitution against `output-small/` was applicable.

## Deviations / calls

- **Three mines, one session** — same deviation as the 1st session, same justification: the dedup inventory compounded across mines (CE → intent → router, each informing the next's reader prompts), and readers kept raw material out of the main window. Second session at this ceiling; the Seq-1 and Seq-2 sessions should each be fresh.
- **No decisions logged** — deliberate: all adoptions were row/ticket/doc landings under existing gates; the two real forks (500-cap, deletion-test) stay routed to the consolidation pass. D-072 marker untouched and current.
