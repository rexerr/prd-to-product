# Retro — 2026-06-17 ~20:45 CDT — CF-03 adoption (first Wave-1 crib) + first live furnace→Cowork ledger write   (4th session of the day)

## What was completed

- **Full end-to-end read of [`cribs-from-pocock-craft.md`](../cribs-from-pocock-craft.md)** (the at-most-once consideration the 3rd-session retro flagged: only grep-verified, never read as prose). Verdict: safe to adopt Wave 1 from — clean consolidation, no dangling merged-id references, the `CF-06` category-error repair held. Surfaced two structural (non-blocking) gaps: the local "Recommended sequencing" section predates Round 2 and was never updated, and two "investigate" pools coexist. Added a one-line **supersession note** atop that section pointing at the roadmap (D-022) and flagging both for the roadmap to absorb.
- **Adopted `CF-03`** (red-capable-repro gate + debug-log hygiene) — the first crib adopted through the [D-022](../DECISIONS.md) Wave-1 lifecycle. Rewrote CLAUDE.md's one-sentence "Reproduce before fixing" rule (1→5 sentences): requires a *red-capable* repro (asserts the exact symptom, run once and watched fail), adapted to this runner-less repo (hook live-fire / dry-run diff against the qualified `skills/context-engineering/examples/output-small/`), inline cross-ref to "Verification before claiming done", one-line debug-log-prefix clause. Now cites two failure modes — repaying a latent arch-rule-2 gap (the old rule cited none).
- **Lifecycle bookkeeping** (D-022 step 5): flipped `CF-03` → `Adopted (→ D-023)` in its source tracker; logged **D-023** in DECISIONS.md; refreshed the roadmap trackers-row + sync marker. Authored via `/furnace-plan`, routed through Cowork (no must-fix; two refinements applied).

## Failure this session

- **Dominant tag: none.** No bad substitution (verified red/green on every claim), no scope creep (the 4-file footprint is the *confirmed* standing D-022 lifecycle shape, not creep — Rex/Cowork signed off), no lost context, no goal drift.
- **Honest process observation (furnace underperformance, prompt-level not kill):** the draft verification ledger asserted the new path token "matches line 72" — but line 72 carries the *fully-qualified* path and the draft used the *abbreviated* form, so it was only a substring match. Cowork caught this as a **bucket-1** finding (a no-read-behind-it claim: I asserted a string match without reading line 72's exact spelling). The furnace preflight's Check 1 ("cite every codebase claim to a read") *should* structurally catch this class and didn't — the read happened but the comparison was imprecise. This is the forcing-function underperforming on a sub-case → tighten the preflight's string-claim discipline, not a kill. Fixed pre-edit (qualified the path; grep step 3 now pins ≥2 verbatim hits).

## The headline event — positive-path furnace ledger write, live

- BACKLOG's furnace item listed as **"still pending: the positive path — a furnace plan with a `## Verification ledger` actually producing a row — fires on the next real `/furnace-plan` → Cowork run."** **It fired this session.** Cowork's `/plan-review` appended **two non-`[backfilled]` rows** to [`skills/furnace-plan/trial-ledger.md`](../../skills/furnace-plan/trial-ledger.md) for the CF-03 review: one **bucket-1 refinement** (the path-imprecision above) and one **bucket-3 refinement** (tighten the always-loaded-file prose against the CF-01-diagnosed sprawl). This is the first end-to-end exercise of the live Cowork write mechanism, not a hand-backfill.
- **Swept into its own dedicated commit** per CLAUDE.md's D-018 carve-out ("Sweep Cowork ledger append", scoped strictly to that one file).
- **Routed to Rex, not self-graded:** classifying those two rows into the furnace trial's promotion/kill buckets is the grading work Rex owns (he's the oracle; the furnace can't grade itself). Flagged below — I did **not** edit the BACKLOG furnace verdict or its "still pending" line.

## What verification covered — and what it didn't

- **Covered (class D + a pre-edit confirm):** all 7 plan verify steps ran green — core sentence preserved (grep=1), cross-ref header resolves (grep=1) and is cited by the new rule, qualified path appears exactly twice (line 72 + new rule) with no abbreviated leak, `git diff` shows only the CF-03 row changed in the tracker (the supersession note is the session's other intended edit), `### D-023` appears exactly once above the footer, roadmap line 15 untouched / line 16 updated. Cleared the one ledgered UNVERIFIED by `ls`-ing the path on disk before editing.
- **Did NOT cover:** I did not click-test that every inline link *inside the new D-023 entry* resolves (e.g. the `[D-022](DECISIONS.md)` self-link form); low risk — the link forms mirror D-020/D-021/D-022 which are known-good. Re-read the new rule for prose flow but did not re-read the full CLAUDE.md top-to-bottom for any second-order interaction.

## Files changed

- `CLAUDE.md` — "Reproduce before fixing" rule rewritten (1→5 sentences, two failure modes cited).
- `docs/cribs-from-pocock-craft.md` — CF-03 Status → `Adopted (→ D-023)`; + the earlier supersession note atop "Recommended sequencing".
- `docs/DECISIONS.md` — **D-023** added before the footer (not mirrored to ACTIVE — visible-by-reading precedent).
- `docs/cribs-adoption-roadmap.md` — trackers-row + sync marker reflect the adoption.
- `skills/furnace-plan/trial-ledger.md` — Cowork's 2 live rows (**separate dedicated commit**).

## Key decisions made

- **D-023** — CF-03 adopted; "Reproduce before fixing" gains the red-capable-repro gate + debug-log hygiene, adapted to the runner-less repo. Class-(D) verified. Not mirrored to DECISIONS_ACTIVE.

## Open items

- **RESOLVED this session (Rex cleared the gate — "don't gate on me, do 1 and 2"):** classified the 2 live Cowork rows (1 bucket-1 + 1 bucket-3, no bucket-2) and updated the BACKLOG furnace item — positive path now **live-verified**, row count 18→20. **Deliberately did NOT edit the furnace `SKILL.md` on the bucket-1**: the trial is under active measurement, and the bucket-1 "tighten the preflight" rule is a *pattern* response ("*mostly* bucket 1"); patching the instrument on n=1 would violate Rule of Two and contaminate the signal. Instead recorded the evidence + a concrete firing trigger in BACKLOG (a *second* bucket-1 string/equality miss → then add "quote BOTH sides of a string-match claim" to Check 1). That is the disciplined resolution of item 2, not a dodge.
- **Wave 1 continues:** next cheap wins are `CF-02` (durable-PRD rules → prd-creator), `CF-06` (de-dup-by-reference → retro template + end-session), `CF-04` (concept-keyed declined-ledger), plus the steinberger batch (`C-14`/`C-15`/`C-09`/`C-10`).
- **Roadmap cleanup** (from the full read): the pocock-craft local sequencing section + dual investigate pools want folding into the roadmap on its next touch (flagged in-place, not yet done).

## Next session

- Pull the next Wave-1 crib from [`cribs-adoption-roadmap.md`](../cribs-adoption-roadmap.md). Recommended: `CF-06` (smallest, one line, closes a live retro/DECISIONS drift hazard) or `CF-02`.
