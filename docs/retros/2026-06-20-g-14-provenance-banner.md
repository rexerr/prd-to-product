# Retro — 2026-06-20 ~12:50 CDT — G-14 adopted (generated-file provenance banner)   (1st session of the day)

## What was completed

- Adopted crib **`G-14`** via `/furnace-plan` → blind `Explore` review → two Cowork `/plan-review` rounds → implementation. Every file the `context-engineering` generator emits now leads with a one-line provenance banner — *"Generated by the context-engineering skill — safe to hand-edit. Re-running the skill regenerates the scaffold shape and asks before overwriting your changes."* — gstack's wording **inverted** ("safe to hand-edit," not "do not edit") because the D-005/D-006 write-guard already owns clobber-safety.
- **Surface:** all **28 templates** (HTML comment for markdown, `<!-- -->` after the closing `---` for the 5 frontmatter rules, two `#` lines after the shebang for the 3 hooks, `#` at top for codex TOML; the one un-commentable format — `settings.json` — carries it via its existing `"//"` key, wording aligned) + the **14 `output-small/` fixtures**.
- **The load-bearing addition** (Cowork's round-1 Must-fix): codified an explicit **strip-vs-banner discriminator** in [`generator/decisions.md`](../../skills/context-engineering/generator/decisions.md) — banner identified by its `Generated by…` text and emitted; the `<!-- Template: … -->` authoring block identified by its `Template:`/`Purpose:` lines and stripped; banner placed immediately after the block in source; the frontmatter strip consumes the leading blank (round-2 refinement). This **also documents for the first time** that the authoring-metadata block was being stripped at emit at all (previously by inference). Surfaced in [`SKILL.md`](../../skills/context-engineering/SKILL.md) "Annotation conventions" so a future template author finds both halves.
- Logged [D-046](../DECISIONS.md); bumped the DECISIONS_ACTIVE reconciliation marker (recording D-045 *and* D-046 as evaluated-not-mirrored); flipped `G-14` `Proposed → Adopted` in both tracker tables; refreshed the roadmap marker → **Wave-2 Sprint 3 complete**.

## Failure this session

- **Tag:** none.
- No bad-substitution / scope-creep / lost-context / goal-drift landed in the implementation. The furnace preflight passed with no UNVERIFIED; the implementation was clean and fully verified.
- **Honest note (the loop working as designed):** the furnace preflight *and* the blind in-session subagent both **missed** the strip-vs-banner discriminator gap that Cowork caught as a round-1 **Must-fix** (logged bucket-3 in the trial-ledger). This is exactly the bucket-2-adjacent class the loop exists for — a completeness gap in the *codification* (not a false claim) that a fresh adversarial read surfaced. It was caught and fixed **before any code was written**, across two plan-review rounds, so nothing reached the working tree wrong. Cost: two extra plan rounds; benefit: a deterministic strip rule instead of an ambiguous one.

## Verification

- Banner present in **28/28 templates and 14/14 fixtures** (grep-guarded); `settings.json` re-validated as parseable JSON.
- Two **un-fixtured placement rules** spot-checked by hand (the honest coverage gap — `output-small/` is flat-shape with no `.codex/`): frontmatter (`ai-shared`) → banner lands directly after the closing `---`, YAML frontmatter intact; TOML (`codex-config`) → banner at emitted top, structure intact. Emit was **simulated** (no executable generator exists) and the strip rule was independently exercised by a **fresh blind agent** given only the discriminator rule + template tops (no fixtures, no expected answer) — it reproduced all three representative emits correctly and flagged the shell "no authoring block" clarity point, which I folded into `decisions.md`.
- All fixture positions asserted (markdown line 1 / shell line 2 / JSON `"//"`); D-046 cross-references resolve.

## Files changed

50 product files (see [D-046](../DECISIONS.md) and `git diff` — reference-don't-restate): 42 banner inserts (28 templates + 14 fixtures), `generator/decisions.md` (new section + discriminator), `SKILL.md`, `DECISIONS.md` (D-046), `DECISIONS_ACTIVE.md` (marker), `cribs/cribs-from-gstack.md` (×2 rows), `cribs-adoption-roadmap.md`, two abbreviated sketches. The Cowork `trial-ledger.md` append (8 rows) is swept separately per the CLAUDE.md carve-out.

## Notable — first live `cc-subagent` proof

This plan's blind in-session `Explore` reviewer ([D-043](../DECISIONS.md)) produced a `## Subagent review log`, and **Cowork transcribed its three findings into `cc-subagent` rows** in `trial-ledger.md` — the first end-to-end firing of the Plan-2 loop that BACKLOG flagged as still-pending ("first live cc-subagent run … confirm Cowork transcribes them"). The mechanism works: blind subagent catches format/clarity items in-session, Cowork attributes them to `cc-subagent`, and the authoritative bucket-2 Must-fix (the discriminator) came from Cowork — the division of labour the trial predicted.

## Next session

- **S3 is done.** Next crib work = **S4** (DSB `G-19` — memorable-thing anchor + falsifiable design rationale) and solo **`CF-07`**. Or switch tracks to the parked **Context-lifecycle** foundational rock (higher-leverage; see [`BACKLOG.md`](../../BACKLOG.md)).
- This session's work is **committed** (G-14 product commit + a dedicated ledger-sweep commit), **not pushed** — push when asked.
